---
phase: 01-graph-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - CLAUDE.md
autonomous: false
requirements: [AUTO-01]

must_haves:
  truths:
    - "CLAUDE.md contains architecture overview of the graph module"
    - "CLAUDE.md contains critical constraints (mutation-via-API, no direct NetworkX access, ALLOWED_PROPERTY_TYPES)"
    - "CLAUDE.md contains validation protocols (test commands, lint commands)"
    - "CLAUDE.md contains script catalog (CLI commands, test commands)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Complete project autonomy documentation"
      contains: "## Architecture"
  key_links:
    - from: "CLAUDE.md"
      to: "src/token_world/graph/"
      via: "documented API reference and constraints"
      pattern: "KnowledgeGraph"
---

<objective>
Update the project-level CLAUDE.md with architecture overview, critical constraints, validation protocols, and script catalog so any agent (human or AI) can understand and work on the project autonomously.

Purpose: AUTO-01 requires CLAUDE.md to be the single source of truth for how to work on this project. After Phase 1, the graph module is the most complex subsystem and needs documentation.
Output: Updated CLAUDE.md with graph architecture, API reference, constraints, and commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-graph-foundation/01-CONTEXT.md
@.planning/phases/01-graph-foundation/01-01-SUMMARY.md
@CLAUDE.md
@docs/design/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md with graph architecture and project autonomy docs</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md,
    src/token_world/graph/__init__.py,
    src/token_world/graph/knowledge_graph.py,
    src/token_world/graph/models.py,
    src/token_world/graph/persistence.py,
    src/token_world/graph/identity.py,
    docs/design/architecture.md,
    pyproject.toml
  </read_first>
  <action>
    Update the existing CLAUDE.md. Preserve ALL existing content (project description, agent autonomy, operating principles, documentation maintenance, code quality, GSD workflow enforcement, technology stack, constraints, conventions sections). Add/update the following sections:

    **Update `## Architecture` section** (currently says "Architecture not yet mapped"):
    Replace with actual architecture content:
    - **Graph Module** (`src/token_world/graph/`): Brief description of each file:
      - `knowledge_graph.py` — `KnowledgeGraph` class wrapping NetworkX DiGraph. All mutations logged as events. Two node types: `agent` and `entity` (D-01). `claim_id()` for readable unique IDs (D-02).
      - `persistence.py` — `GraphPersistence` SQLite adapter. Stores graph as JSON blob via `json_graph.node_link_data`. Manages snapshots table.
      - `events.py` — `GraphEvent` dataclass and `EventStore`. All mutations produce events for audit trail.
      - `identity.py` — `claim_id()` deconfliction. Proposes readable ID, appends hash suffix on collision.
      - `models.py` — `Mutation` and `SnapshotInfo` frozen dataclasses. `ALLOWED_PROPERTY_TYPES` constant.
    - **Universe Module** (`src/token_world/universe/`): Already exists — UniverseManager, scaffolding, templates, MCP stub.
    - **Key invariant:** All graph mutations go through KnowledgeGraph API. Never call `nx.DiGraph` methods directly. This ensures event logging, property validation, and persistence correctness.

    **Update `## Conventions` section** (currently says "not yet established"):
    Replace with actual conventions:
    - **Graph mutations:** Always through `KnowledgeGraph` methods (`add_node`, `add_edge`, `set`, `remove_node`, `remove_edge`). Never direct NetworkX access.
    - **Node types:** Only `"agent"` and `"entity"`. Everything else is emergent properties.
    - **Property values:** Must be JSON-serializable: `str, int, float, bool, None, list, dict`. No custom objects, sets, tuples, bytes.
    - **Node IDs:** Use `kg.claim_id("name")` to get a unique, human-readable ID. Never hardcode IDs that might collide.
    - **Snapshots:** Linked to tick IDs. Summary describes changes since last snapshot. Max 50 retained.
    - **SQLite:** Raw `sqlite3` with parameterized queries. No ORM. Context manager pattern: `with sqlite3.connect(str(path)) as conn:`.
    - **Testing:** `GraphBuilder` in `tests/test_graph/conftest.py` for fluent graph construction. Use `kg` fixture for KnowledgeGraph with temp DB. Use `graph_builder` fixture for builder pattern.
    - **Imports:** Use `from token_world.graph import KnowledgeGraph, Mutation` (public API via `__init__.py`).

    **Add `## Validation Protocols` section** after Conventions:
    ```
    ## Validation Protocols

    - **Quick test:** `uv run pytest tests/ -x -q` (stop on first failure)
    - **Full test:** `uv run pytest -v` (verbose, all tests)
    - **Graph tests only:** `uv run pytest tests/test_graph/ -x -q`
    - **Lint:** `uv run ruff check src/`
    - **Format check:** `uv run ruff format --check src/`
    - **Format fix:** `uv run ruff format src/`
    - **Type check:** `uv run mypy src/token_world/graph/`

    Run quick test after every change. Run full suite before commits.
    ```

    **Add `## Script Catalog` section** after Validation Protocols:
    ```
    ## Script Catalog

    | Command | Purpose |
    |---------|---------|
    | `token-world create "Name"` | Create a new universe with scaffolding |
    | `token-world list` | List all universes with metadata |
    | `token-world delete slug` | Delete a universe |
    | `uv run pytest -x -q` | Quick test run |
    | `uv run pytest -v` | Full verbose test run |
    | `uv run ruff check src/` | Lint check |
    | `uv run ruff format src/` | Auto-format |
    | `uv run mypy src/token_world/graph/` | Type check graph module |
    ```

    **Add `## Critical Constraints` section** after Script Catalog:
    ```
    ## Critical Constraints

    1. **Graph is ground truth** — If it's not in the KnowledgeGraph, it doesn't exist. No side channels, no implicit state.
    2. **Mutation-mediated access** — All graph changes go through KnowledgeGraph API. This ensures event logging, validation, and persistence correctness. Direct NetworkX DiGraph access breaks these guarantees.
    3. **Two node types only** — Framework enforces `agent` and `entity`. All other classification is emergent (mechanics add properties like `subtype="weapon"`).
    4. **JSON-serializable properties** — Property values must survive `json.dumps/loads` roundtrip. No Python-specific types (set, tuple, bytes, custom objects).
    5. **Snapshot-linked ticks** — Every snapshot references a tick ID. Rollback restores to a tick's state. Events before oldest snapshot may be compacted.
    6. **No ORM** — Raw sqlite3 only. SQLAlchemy is explicitly forbidden.
    7. **No pickle** — All serialization via JSON. Pickle is explicitly forbidden.
    ```
  </action>
  <verify>
    <automated>grep -c "## Architecture" CLAUDE.md && grep -c "## Conventions" CLAUDE.md && grep -c "## Validation Protocols" CLAUDE.md && grep -c "## Script Catalog" CLAUDE.md && grep -c "## Critical Constraints" CLAUDE.md && grep -c "KnowledgeGraph" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `## Architecture` with text mentioning `knowledge_graph.py`, `persistence.py`, `events.py`, `identity.py`, `models.py`
    - CLAUDE.md contains `## Conventions` with text mentioning `add_node`, `claim_id`, `ALLOWED_PROPERTY_TYPES`
    - CLAUDE.md contains `## Validation Protocols` with `uv run pytest` and `uv run ruff`
    - CLAUDE.md contains `## Script Catalog` with `token-world create` and `uv run pytest`
    - CLAUDE.md contains `## Critical Constraints` with text mentioning `Mutation-mediated access`
    - CLAUDE.md still contains `## Agent Autonomy` (existing content preserved)
    - CLAUDE.md still contains `## Operating Principles` (existing content preserved)
    - CLAUDE.md still contains `## Technology Stack` (existing content preserved)
    - CLAUDE.md still contains `## GSD Workflow Enforcement` (existing content preserved)
  </acceptance_criteria>
  <done>
    CLAUDE.md contains architecture overview, conventions, validation protocols, script catalog, and critical constraints (AUTO-01). All existing content preserved. Any agent reading CLAUDE.md can understand the project structure, run tests, and follow conventions without human guidance.
  </done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Verify CLAUDE.md completeness</name>
  <files>CLAUDE.md</files>
  <action>
    Human reviews CLAUDE.md for completeness per AUTO-01. This is a non-blocking checkpoint — phase can proceed regardless.

    What was built: Updated CLAUDE.md with architecture overview, conventions, validation protocols, script catalog, and critical constraints for agent autonomy (AUTO-01).

    How to verify:
    1. Open CLAUDE.md in the project root
    2. Verify it contains these sections: Architecture, Conventions, Validation Protocols, Script Catalog, Critical Constraints
    3. Verify Architecture section describes the graph module files (knowledge_graph.py, persistence.py, events.py, identity.py, models.py)
    4. Verify Conventions section covers mutation-mediated access, node types, property types, claim_id usage
    5. Verify existing sections (Agent Autonomy, Operating Principles, Technology Stack, etc.) are preserved
    6. Run `uv run pytest -x -q` to confirm all tests still pass

    Resume signal: Type "approved" or describe any content gaps.
  </action>
  <verify>
    <automated>grep -c "## Architecture" CLAUDE.md && grep -c "## Critical Constraints" CLAUDE.md && grep -c "KnowledgeGraph" CLAUDE.md</automated>
  </verify>
  <done>Human has reviewed CLAUDE.md and confirmed it contains sufficient documentation for agent autonomy, or described gaps to address.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan, no runtime boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | CLAUDE.md | accept | CLAUDE.md is a public repo file. No secrets or credentials documented. All content is architectural documentation. |
</threat_model>

<verification>
1. CLAUDE.md contains all required sections per AUTO-01
2. Existing CLAUDE.md content is preserved
3. `uv run pytest -q` still passes (no code changes)
</verification>

<success_criteria>
- CLAUDE.md has architecture overview sufficient for an agent to understand the graph module (AUTO-01)
- CLAUDE.md has critical constraints that prevent common mistakes
- CLAUDE.md has validation protocols with exact commands
- CLAUDE.md has script catalog for common operations
- All existing CLAUDE.md content preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-graph-foundation/01-03-SUMMARY.md`
</output>
