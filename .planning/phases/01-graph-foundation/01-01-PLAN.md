---
phase: 01-graph-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/token_world/graph/__init__.py
  - src/token_world/graph/knowledge_graph.py
  - src/token_world/graph/identity.py
  - src/token_world/graph/models.py
  - src/token_world/graph/persistence.py
  - src/token_world/graph/events.py
  - tests/test_graph/__init__.py
  - tests/test_graph/conftest.py
  - tests/test_graph/test_knowledge_graph.py
  - tests/test_graph/test_identity.py
  - tests/test_graph/test_persistence.py
autonomous: true
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, TEST-06]

must_haves:
  truths:
    - "A node can have any JSON-serializable property set at runtime without schema declaration"
    - "Edges can have arbitrary properties (relation type, weight, etc.) without schema declaration"
    - "New properties appear dynamically when set — no migration or registration needed"
    - "claim_id() returns the proposed name if available, or a deconflicted name_XX if taken"
    - "Graph state persists to SQLite and survives process restarts"
    - "GraphBuilder lets tests construct graph scenarios in 2-3 lines"
  artifacts:
    - path: "src/token_world/graph/knowledge_graph.py"
      provides: "KnowledgeGraph class wrapping NetworkX DiGraph"
      exports: ["KnowledgeGraph"]
    - path: "src/token_world/graph/identity.py"
      provides: "claim_id() deconfliction helper"
      exports: ["claim_id"]
    - path: "src/token_world/graph/models.py"
      provides: "Mutation, SnapshotInfo Pydantic/dataclass models"
      exports: ["Mutation", "ALLOWED_PROPERTY_TYPES"]
    - path: "src/token_world/graph/persistence.py"
      provides: "SQLite adapter for graph save/load"
      exports: ["GraphPersistence"]
    - path: "src/token_world/graph/events.py"
      provides: "GraphEvent model and event storage"
      exports: ["GraphEvent"]
    - path: "tests/test_graph/conftest.py"
      provides: "GraphBuilder fixture and tmp_db fixture"
      exports: ["graph_builder", "tmp_db"]
  key_links:
    - from: "src/token_world/graph/knowledge_graph.py"
      to: "src/token_world/graph/events.py"
      via: "mutation logging on every add_node/add_edge/set/remove call"
      pattern: "GraphEvent"
    - from: "src/token_world/graph/knowledge_graph.py"
      to: "src/token_world/graph/persistence.py"
      via: "save() and load() methods delegating to GraphPersistence"
      pattern: "GraphPersistence"
    - from: "src/token_world/graph/knowledge_graph.py"
      to: "src/token_world/graph/identity.py"
      via: "claim_id() method delegates to identity module"
      pattern: "claim_id"
---

<objective>
Build the core KnowledgeGraph class with mutation-mediated access, claim_id() identity system, event logging, SQLite persistence, and test infrastructure including GraphBuilder.

Purpose: This is the foundation layer — all simulation state lives in this graph. Mechanics (Phase 2), the engine (Phase 5), and agents (Phase 6) all interact with the world through this API.
Output: Working KnowledgeGraph with add_node/add_edge/set/remove/query operations, claim_id deconfliction, event logging, SQLite persistence, and GraphBuilder test helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-graph-foundation/01-CONTEXT.md
@.planning/phases/01-graph-foundation/01-RESEARCH.md
@docs/design/architecture.md

<interfaces>
<!-- Existing code patterns the executor needs -->

From src/token_world/universe/manager.py (SQLite pattern):
```python
import sqlite3
from pathlib import Path

# Pattern: raw sqlite3 with context manager
with sqlite3.connect(str(db_path)) as conn:
    conn.execute("CREATE TABLE IF NOT EXISTS ...")
    conn.executemany("INSERT INTO ...", [...])
```

From src/token_world/models.py (Pydantic pattern):
```python
from pydantic import BaseModel, field_validator

class UniverseMetadata(BaseModel):
    name: str
    slug: str
    created_at: datetime
    schema_version: int = 1
```

From tests/conftest.py (fixture pattern):
```python
@pytest.fixture
def tmp_data_dir(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> Path:
    monkeypatch.setenv("XDG_DATA_HOME", str(tmp_path))
    ...
```

From docs/design/architecture.md (KnowledgeGraph API shape):
```python
class KnowledgeGraph:
    def query(node, property) -> Any
    def set(node, property, value) -> Mutation
    def add_node(id, **props) -> Mutation
    def add_edge(src, dst, **props) -> Mutation
    def snapshot() -> GraphState
    def restore(snapshot) -> void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Core KnowledgeGraph, identity, models, events, and test infrastructure</name>
  <files>
    src/token_world/graph/__init__.py,
    src/token_world/graph/models.py,
    src/token_world/graph/events.py,
    src/token_world/graph/identity.py,
    src/token_world/graph/knowledge_graph.py,
    tests/test_graph/__init__.py,
    tests/test_graph/conftest.py,
    tests/test_graph/test_knowledge_graph.py,
    tests/test_graph/test_identity.py
  </files>
  <read_first>
    docs/design/architecture.md,
    src/token_world/models.py,
    src/token_world/universe/manager.py,
    tests/conftest.py,
    .planning/phases/01-graph-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_add_node_agent: KnowledgeGraph().add_node("bob", node_type="agent") stores node with type="agent"
    - test_add_node_entity: KnowledgeGraph().add_node("sword", node_type="entity") stores node with type="entity"
    - test_add_node_requires_type: add_node without node_type raises ValueError
    - test_add_node_only_agent_or_entity: add_node with node_type="location" raises ValueError (per D-01)
    - test_arbitrary_properties: add_node("bob", node_type="agent", hp=100, mood="happy") then query("bob", "hp") returns 100
    - test_set_property: set("bob", "temperature", 98.6) then query("bob", "temperature") returns 98.6
    - test_emergent_property: set any property name without declaration — no error, persists correctly
    - test_property_type_validation: set("bob", "x", set()) raises TypeError (sets not JSON-serializable, per D-04)
    - test_allowed_types: str, int, float, bool, None, list, dict all accepted as property values
    - test_add_edge: add_edge("bob", "sword", relation="holds") creates directed edge with properties
    - test_edge_arbitrary_properties: edges accept any JSON-serializable properties
    - test_query_node: query("bob") returns dict of all properties
    - test_query_node_property: query("bob", "hp") returns just the value
    - test_query_missing_node: query("nonexistent") raises KeyError
    - test_has_node: has_node("bob") returns True/False
    - test_has_edge: has_edge("bob", "sword") returns True/False
    - test_neighbors: neighbors("bob") returns list of adjacent node IDs
    - test_nodes_filter: nodes(type="agent") returns only agent nodes
    - test_remove_node: remove_node("bob") removes node and its edges
    - test_remove_edge: remove_edge("bob", "sword") removes just the edge
    - test_mutation_return: every mutation method returns a Mutation dataclass with type, target, old_value, new_value
    - test_event_logging: after mutations, the internal event log contains corresponding GraphEvent entries
    - test_deepcopy_mutable_values: setting a dict property deepcopies it so mutations to the original dict don't affect graph state
    - test_claim_id_available: claim_id("wallet") returns "wallet" when no node with that ID exists
    - test_claim_id_collision: add node "wallet", then claim_id("wallet") returns "wallet_XX" (suffix)
    - test_claim_id_multiple_collisions: repeated collisions produce unique IDs with progressively longer suffixes
    - test_graph_builder: GraphBuilder().node("bob", type="agent", hp=100).node("sword", type="entity").edge("bob", "sword", relation="holds").build() returns populated KnowledgeGraph
    - test_graph_builder_chaining: builder methods return self for fluent API
  </behavior>
  <action>
    **Create `src/token_world/graph/models.py`:**
    - Define `ALLOWED_PROPERTY_TYPES = (str, int, float, bool, type(None), list, dict)` as module constant
    - Define `Mutation` as a frozen dataclass with fields: `type: str` (one of "add_node", "add_edge", "set_property", "remove_node", "remove_edge"), `target: str` (node_id or "src->dst"), `property: str | None`, `old_value: Any`, `new_value: Any`
    - Define `SnapshotInfo` as a frozen dataclass with fields: `snapshot_id: int`, `tick_id: int`, `summary: str`, `node_count: int`, `edge_count: int`, `created_at: str`

    **Create `src/token_world/graph/events.py`:**
    - Define `GraphEvent` as a frozen dataclass with fields: `tick_id: int`, `event_type: str`, `target_id: str`, `property_name: str | None`, `old_value_json: str | None`, `new_value_json: str | None`
    - Define `EventStore` class with methods: `append(event: GraphEvent)`, `get_events(tick_id: int | None = None) -> list[GraphEvent]`, `clear_before(tick_id: int)`, `__len__() -> int`
    - EventStore stores events in an in-memory list (SQLite persistence added in Task 2)

    **Create `src/token_world/graph/identity.py`:**
    - Implement `claim_id(graph: nx.DiGraph, name: str) -> str` per D-02:
      - If `name` not in graph, return `name`
      - Otherwise, try suffixes of increasing length (2, 4, 6, 8 hex chars) using `hashlib.sha256(f"{name}_{graph.number_of_nodes()}".encode()).hexdigest()[:length]`
      - Return `f"{name}_{hash_suffix}"` for first non-colliding candidate
      - Raise `ValueError(f"Cannot deconflict ID after 4 attempts: {name}")` if all collide

    **Create `src/token_world/graph/knowledge_graph.py`:**
    - Import `nx`, `copy.deepcopy`, `json`, and local modules (models, events, identity)
    - Class `KnowledgeGraph` with `__init__(self, db_path: Path | None = None)`:
      - `self._graph: nx.DiGraph = nx.DiGraph()`
      - `self._events: EventStore = EventStore()`
      - `self._db_path = db_path`
      - `self._current_tick: int = 0`

    - **Query API (read-only, no events):**
      - `query(self, node_id: str, property: str | None = None) -> Any`: If property is None, return dict of all node properties. If property given, return that property's value. Raise `KeyError` if node doesn't exist.
      - `has_node(self, node_id: str) -> bool`
      - `has_edge(self, src: str, dst: str) -> bool`
      - `neighbors(self, node_id: str) -> list[str]`
      - `nodes(self, **filters) -> list[str]`: Filter nodes by property values. E.g., `nodes(type="agent")` returns IDs of all nodes where `type == "agent"`.

    - **Mutation API (logged as events):**
      - `add_node(self, node_id: str, *, node_type: str, **props) -> Mutation`:
        - Validate `node_type in ("agent", "entity")` per D-01, raise `ValueError` otherwise
        - Validate all property values against `ALLOWED_PROPERTY_TYPES` per D-04, raise `TypeError` for invalid types
        - `deepcopy` any mutable values (list, dict) before storing
        - Add node to internal DiGraph with `type=node_type` plus `**props`
        - Log GraphEvent with event_type="add_node"
        - Return `Mutation(type="add_node", target=node_id, property=None, old_value=None, new_value={"type": node_type, **props})`
      - `add_edge(self, src: str, dst: str, **props) -> Mutation`:
        - Validate property values against ALLOWED_PROPERTY_TYPES
        - deepcopy mutable values
        - Add edge to DiGraph
        - Log GraphEvent with event_type="add_edge"
        - Return Mutation with target=f"{src}->{dst}"
      - `set(self, node_id: str, property: str, value: Any) -> Mutation`:
        - Validate value against ALLOWED_PROPERTY_TYPES
        - deepcopy if mutable
        - Record old_value before overwrite
        - Log GraphEvent with event_type="set_property"
        - Return Mutation with old and new values
      - `remove_node(self, node_id: str) -> Mutation`: Remove node and all edges, log event, return Mutation
      - `remove_edge(self, src: str, dst: str) -> Mutation`: Remove edge, log event, return Mutation

    - **Identity:**
      - `claim_id(self, name: str) -> str`: Delegates to `identity.claim_id(self._graph, name)`

    - **Tick management:**
      - `set_tick(self, tick_id: int) -> None`: Set current tick for event logging
      - `property current_tick -> int`: Return current tick

    **Create `src/token_world/graph/__init__.py`:**
    - Export `KnowledgeGraph` from `knowledge_graph`, `Mutation` and `ALLOWED_PROPERTY_TYPES` from `models`, `claim_id` from `identity`

    **Create `tests/test_graph/__init__.py`:** Empty init file.

    **Create `tests/test_graph/conftest.py`:**
    - `@pytest.fixture def kg(tmp_path)` -> returns `KnowledgeGraph(db_path=tmp_path / "test.db")`
    - `@pytest.fixture def tmp_db(tmp_path)` -> returns `tmp_path / "test.db"` (for persistence tests)
    - Define `GraphBuilder` class with fluent API:
      - `node(self, id: str, *, node_type: str = "entity", **props) -> GraphBuilder`: Stores node spec
      - `edge(self, src: str, dst: str, **props) -> GraphBuilder`: Stores edge spec
      - `build(self) -> KnowledgeGraph`: Creates KnowledgeGraph, adds all nodes and edges, returns it
    - `@pytest.fixture def graph_builder()` -> returns `GraphBuilder()` instance

    **Create `tests/test_graph/test_knowledge_graph.py`:**
    - Test all behaviors listed in `<behavior>` above for KnowledgeGraph operations
    - Group into classes: `TestAddNode`, `TestQuery`, `TestMutation`, `TestEdges`, `TestRemove`, `TestEventLogging`, `TestPropertyValidation`, `TestGraphBuilder`

    **Create `tests/test_graph/test_identity.py`:**
    - Test claim_id available, collision, multiple collisions per behavior list
  </action>
  <verify>
    <automated>uv run pytest tests/test_graph/test_knowledge_graph.py tests/test_graph/test_identity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/token_world/graph/knowledge_graph.py contains `class KnowledgeGraph`
    - src/token_world/graph/knowledge_graph.py contains `def add_node(self, node_id: str, *, node_type: str`
    - src/token_world/graph/knowledge_graph.py contains `if node_type not in ("agent", "entity")`
    - src/token_world/graph/models.py contains `ALLOWED_PROPERTY_TYPES = (str, int, float, bool, type(None), list, dict)`
    - src/token_world/graph/models.py contains `class Mutation`
    - src/token_world/graph/identity.py contains `def claim_id(`
    - src/token_world/graph/events.py contains `class GraphEvent`
    - src/token_world/graph/events.py contains `class EventStore`
    - tests/test_graph/conftest.py contains `class GraphBuilder`
    - tests/test_graph/conftest.py contains `def build(self) -> KnowledgeGraph`
    - tests/test_graph/test_knowledge_graph.py contains `test_arbitrary_properties`
    - tests/test_graph/test_knowledge_graph.py contains `test_emergent_property`
    - tests/test_graph/test_identity.py contains `test_claim_id_collision`
    - `uv run pytest tests/test_graph/test_knowledge_graph.py tests/test_graph/test_identity.py -x -q` exits 0
  </acceptance_criteria>
  <done>
    KnowledgeGraph supports add_node (agent/entity only per D-01), add_edge, set, remove, query with arbitrary JSON-serializable properties (per D-03, D-04). claim_id deconflicts per D-02. All mutations logged as events. GraphBuilder fixture enables 2-3 line test setup (TEST-06). All tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLite persistence — save, load, and event persistence</name>
  <files>
    src/token_world/graph/persistence.py,
    src/token_world/graph/knowledge_graph.py,
    tests/test_graph/test_persistence.py
  </files>
  <read_first>
    src/token_world/graph/knowledge_graph.py,
    src/token_world/graph/models.py,
    src/token_world/graph/events.py,
    src/token_world/universe/manager.py,
    .planning/phases/01-graph-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - test_save_empty_graph: save() on empty graph creates SQLite tables without error
    - test_save_load_roundtrip: add nodes and edges, save(), create new KnowledgeGraph from same db_path, load(), all nodes/edges/properties match
    - test_persist_arbitrary_properties: node with int, str, float, bool, None, list, dict properties survives save/load roundtrip
    - test_persist_edge_properties: edge with arbitrary properties survives save/load roundtrip
    - test_persist_survives_restart: save(), delete Python object, create new KnowledgeGraph, load(), all state matches (simulates process restart)
    - test_events_persisted: mutations create events, save() writes events to SQLite, load() restores events
    - test_tables_created_lazily: GraphPersistence creates tables on first save(), not on __init__
    - test_directed_graph_preserved: edges maintain directionality after save/load (per Pitfall 1 from research)
  </behavior>
  <action>
    **Create `src/token_world/graph/persistence.py`:**
    - Import `sqlite3`, `json`, `nx`, `json_graph` from networkx.readwrite, `Path`
    - Class `GraphPersistence`:
      - `__init__(self, db_path: Path)`: Store db_path, no connection yet
      - `_ensure_tables(self, conn: sqlite3.Connection)`: Create tables if not exist:
        ```sql
        CREATE TABLE IF NOT EXISTS graph_state (
            id INTEGER PRIMARY KEY CHECK (id = 1),
            graph_json TEXT NOT NULL,
            current_tick INTEGER NOT NULL DEFAULT 0,
            node_count INTEGER NOT NULL DEFAULT 0,
            edge_count INTEGER NOT NULL DEFAULT 0,
            updated_at TEXT NOT NULL DEFAULT (datetime('now'))
        );

        CREATE TABLE IF NOT EXISTS graph_events (
            event_id INTEGER PRIMARY KEY AUTOINCREMENT,
            tick_id INTEGER NOT NULL,
            event_type TEXT NOT NULL,
            target_id TEXT NOT NULL,
            property_name TEXT,
            old_value_json TEXT,
            new_value_json TEXT,
            created_at TEXT NOT NULL DEFAULT (datetime('now'))
        );

        CREATE INDEX IF NOT EXISTS idx_events_tick ON graph_events(tick_id);

        CREATE TABLE IF NOT EXISTS graph_snapshots (
            snapshot_id INTEGER PRIMARY KEY AUTOINCREMENT,
            tick_id INTEGER NOT NULL,
            summary TEXT NOT NULL DEFAULT '',
            graph_json TEXT NOT NULL,
            node_count INTEGER NOT NULL,
            edge_count INTEGER NOT NULL,
            created_at TEXT NOT NULL DEFAULT (datetime('now')),
            UNIQUE(tick_id)
        );

        CREATE INDEX IF NOT EXISTS idx_snapshots_tick ON graph_snapshots(tick_id);
        ```
      - `save(self, graph: nx.DiGraph, events: list[GraphEvent], current_tick: int)`:
        - Serialize graph: `json.dumps(json_graph.node_link_data(graph), ensure_ascii=False, allow_nan=False)`
        - Use `INSERT OR REPLACE INTO graph_state` (single row, id=1)
        - Insert new events into graph_events table
        - Commit
      - `load(self) -> tuple[nx.DiGraph, list[GraphEvent], int]`:
        - Read graph_state row
        - Deserialize: `json_graph.node_link_graph(json.loads(graph_json), directed=True, multigraph=False)` (per Pitfall 1)
        - Read events from graph_events table
        - Return (graph, events, current_tick)
      - `has_data(self) -> bool`: Check if graph_state table has a row

    **Modify `src/token_world/graph/knowledge_graph.py`:**
    - Import `GraphPersistence`
    - In `__init__`, if `db_path` is not None, create `self._persistence = GraphPersistence(db_path)`, else `self._persistence = None`
    - Add `save(self) -> None`: If persistence exists, call `self._persistence.save(self._graph, self._events.get_events(), self._current_tick)`. Then clear the in-memory event list (events are now persisted).
    - Add `load(self) -> None`: If persistence exists and has_data(), call `self._persistence.load()`, replace `self._graph`, restore events, set `self._current_tick`.

    **Create `tests/test_graph/test_persistence.py`:**
    - Test all behaviors listed in `<behavior>` above
    - Use `tmp_db` fixture from conftest for database paths
    - For restart test: create KnowledgeGraph with db_path, add nodes, save(), then create a NEW KnowledgeGraph with same db_path, load(), assert all state matches
    - For directed test: add edge A->B, save/load, verify A->B exists but B->A does not
  </action>
  <verify>
    <automated>uv run pytest tests/test_graph/test_persistence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - src/token_world/graph/persistence.py contains `class GraphPersistence`
    - src/token_world/graph/persistence.py contains `directed=True, multigraph=False`
    - src/token_world/graph/persistence.py contains `ensure_ascii=False, allow_nan=False`
    - src/token_world/graph/persistence.py contains `CREATE TABLE IF NOT EXISTS graph_state`
    - src/token_world/graph/persistence.py contains `CREATE TABLE IF NOT EXISTS graph_events`
    - src/token_world/graph/persistence.py contains `CREATE TABLE IF NOT EXISTS graph_snapshots`
    - src/token_world/graph/knowledge_graph.py contains `def save(self)`
    - src/token_world/graph/knowledge_graph.py contains `def load(self)`
    - tests/test_graph/test_persistence.py contains `test_save_load_roundtrip`
    - tests/test_graph/test_persistence.py contains `test_persist_survives_restart`
    - tests/test_graph/test_persistence.py contains `test_directed_graph_preserved`
    - `uv run pytest tests/test_graph/test_persistence.py -x -q` exits 0
    - `uv run pytest tests/test_graph/ -x -q` exits 0 (all graph tests still pass)
  </acceptance_criteria>
  <done>
    Graph state persists to SQLite and survives process restarts (GRAPH-03). Events persist alongside graph state. Directed edge semantics preserved across save/load cycles. Full test suite passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mechanic -> KnowledgeGraph API | Mechanics (untrusted in later phases — LLM-generated code) mutate graph through the API |
| SQLite file I/O | Graph state read/written to SQLite on local filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | KnowledgeGraph.set() | mitigate | Validate property values against ALLOWED_PROPERTY_TYPES before storing; deepcopy mutable values to prevent reference mutation |
| T-01-02 | Tampering | GraphPersistence.load() | mitigate | Use `allow_nan=False` in JSON serialization to reject invalid values; always pass `directed=True, multigraph=False` to `node_link_graph` |
| T-01-03 | Information Disclosure | GraphPersistence SQLite | accept | SQLite file on local filesystem, single-user hobby project. File permissions inherited from OS. No encryption needed for v1. |
| T-01-04 | Denial of Service | KnowledgeGraph.add_node() | accept | No node count limits for v1. Graph expected to be small (< 1000 nodes). Monitor in Phase 3 if needed. |
| T-01-05 | Elevation of Privilege | claim_id() | mitigate | claim_id only returns string IDs — no path traversal or injection possible since IDs are used as dict keys in NetworkX, not filesystem paths |
</threat_model>

<verification>
After both tasks complete:
1. `uv run pytest tests/test_graph/ -x -q` — all graph tests pass
2. `uv run pytest -q` — full suite including Phase 0 tests still passes
3. `uv run ruff check src/token_world/graph/` — no lint errors
4. `uv run ruff format --check src/token_world/graph/` — formatting clean
</verification>

<success_criteria>
- KnowledgeGraph supports arbitrary properties on nodes and edges (GRAPH-01)
- New properties emerge dynamically without schema (GRAPH-02)
- Graph persists to SQLite and survives restart (GRAPH-03)
- GraphBuilder enables concise test setup (TEST-06)
- All mutations return Mutation dataclass and are logged as events
- claim_id() deconflicts per D-02
- Only agent/entity node types accepted per D-01
- All property values validated against JSON-serializable types per D-04
</success_criteria>

<output>
After completion, create `.planning/phases/01-graph-foundation/01-01-SUMMARY.md`
</output>
