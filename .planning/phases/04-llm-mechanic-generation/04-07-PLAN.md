---
phase: 04-llm-mechanic-generation
plan: 07
title: "Seed cluster: spatial queries, AoE, speech, try_door (MECH02 look, MECH03 find_nearest, MECH04 aoe, MECH13 speak, MECH27 try_door)"
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-06
files_modified:
  - src/token_world/mechanic/seeds/look.py
  - src/token_world/mechanic/seeds/find_nearest.py
  - src/token_world/mechanic/seeds/aoe.py
  - src/token_world/mechanic/seeds/speak.py
  - src/token_world/mechanic/seeds/try_door.py
  - src/token_world/mechanic/seeds/_helpers.py
  - tests/test_mechanic/test_seeds/test_look.py
  - tests/test_mechanic/test_seeds/test_find_nearest.py
  - tests/test_mechanic/test_seeds/test_aoe.py
  - tests/test_mechanic/test_seeds/test_speak.py
  - tests/test_mechanic/test_seeds/test_try_door.py
  - .planning/use-cases/spatial/UC-S02-line-of-sight-occlusion.md
  - .planning/use-cases/spatial/UC-S03-nearest-object-query.md
  - .planning/use-cases/spatial/UC-S04-area-of-effect.md
  - .planning/use-cases/social/UC-O08-speech-broadcast.md
  - .planning/use-cases/edge-case/UC-E06-move-into-locked-room.md
  - .planning/phases/04-llm-mechanic-generation/04-VALIDATION.md
autonomous: true
requirements:
  - MECH-03
gap_ids:
  - GAP-MECH02
  - GAP-MECH03
  - GAP-MECH04
  - GAP-MECH13
  - GAP-MECH27
decisions:
  - D-03
  - D-05
  - D-11
  - D-37
threats:
  - T-04-AST-BYPASS
must_haves:
  truths:
    - "look mechanic returns visible neighbors honoring occluders (uses ctx.spatial.segment_intersections if available; falls back to direct-neighbor subtype filter)"
    - "find_nearest mechanic returns the nearest entity matching a filter using ctx.spatial.nearest (or brute force)"
    - "aoe mechanic applies a property delta to all nodes within a radius/shape"
    - "speak mechanic propagates utterance to agents within an earshot radius (uses ctx.spatial.within)"
    - "try_door mechanic reports door state; flips `locked=False` if actor holds a matching key; shares _find_open_passage / refusal-narrative pattern"
    - "UC-S02, UC-S03, UC-S04, UC-O08, UC-E06 flip from yield → pass"
  artifacts:
    - path: "src/token_world/mechanic/seeds/look.py"
      provides: "MECH02 line-of-sight observation"
    - path: "src/token_world/mechanic/seeds/find_nearest.py"
      provides: "MECH03 nearest-X query"
    - path: "src/token_world/mechanic/seeds/aoe.py"
      provides: "MECH04 area-of-effect"
    - path: "src/token_world/mechanic/seeds/speak.py"
      provides: "MECH13 earshot-scoped speech"
    - path: "src/token_world/mechanic/seeds/try_door.py"
      provides: "MECH27 door interaction"
    - path: "src/token_world/mechanic/seeds/_helpers.py"
      provides: "Grows with _try_unlock_with_key helper; refusal narrative helper"
---

<objective>
Ship 5 spatial/social seed mechanics. Three consume Phase-3 spatial queries (`ctx.spatial.segment_intersections`, `.nearest`, `.within`) or fall back gracefully; try_door shares `_find_open_passage` with 04-06. Flip 5 UCs from yield → pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-llm-mechanic-generation/04-CONTEXT.md
@.planning/phases/04-llm-mechanic-generation/04-RESEARCH.md
@.planning/phases/04-llm-mechanic-generation/04-06-SUMMARY.md
@.planning/GAP-ANALYSIS.md
@docs/guides/authoring-mechanics.md
@src/token_world/mechanic/context.py
@src/token_world/graph/spatial.py
@src/token_world/mechanic/seeds/_helpers.py
@src/token_world/mechanic/seeds/passage_move.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author look (MECH02) + find_nearest (MECH03) + aoe (MECH04)</name>
  <read_first>
    - .planning/use-cases/spatial/UC-S02-line-of-sight-occlusion.md
    - .planning/use-cases/spatial/UC-S03-nearest-object-query.md
    - .planning/use-cases/spatial/UC-S04-area-of-effect.md
    - .planning/GAP-ANALYSIS.md §GAP-MECH02, GAP-MECH03, GAP-MECH04
    - src/token_world/graph/spatial.py (SpatialIndex API — know what's available vs what needs fallback)
    - src/token_world/mechanic/context.py (`spatial` lazy property)
    - docs/guides/authoring-mechanics.md §4 MechanicContext DSL Reference, §9 Reactive Cycles
  </read_first>
  <behavior>
    look:
    - id="look"; voluntary=True; tags=["spatial","observation"]
    - check: actor exists + has position (or located_in with a bbox)
    - apply: returns no mutations (read-only like observation); but we can't return observations from apply — so look either writes a `last_observed: list[NodeId]` property on actor OR emits via execution trace. Use the write-to-actor-property approach: `ctx.mutate(actor, "last_observed", [visible_ids])`.
    - Visibility: all positioned entities within direct neighbors or within a `line_of_sight_radius` (e.g., 10 units) EXCEPT nodes whose segment from actor.position is occluded by a node with `occludes=True`.
    - If `ctx.spatial.segment_intersections` exists, use it; else fall back to: neighbors(actor_location) filtered by subtype not "wall"/not opaque.

    find_nearest:
    - id="find_nearest"; voluntary=True; tags=["spatial","query"]
    - check: actor has position; target is a FILTER KEY (string — e.g., ctx.target is used as the desired subtype, or the mechanic reads a `filter_subtype` property from somewhere — simplest: ctx.target is the subtype string to search for)
    - apply: writes `ctx.mutate(actor, "nearest_result", <nearest_id_or_None>)`
    - Uses `ctx.spatial.nearest(point, filter=lambda props: props.get("subtype") == ctx.target, k=1)` if available; else brute-force.

    aoe:
    - id="aoe"; voluntary=True; tags=["spatial","aoe"]
    - check: actor has position; target has a `radius` property or ctx has an `aoe_radius` convention
    - apply: find all nodes within radius (via `ctx.spatial.within` or brute force); for each, apply a `damage` property delta of -5 (or read a parameter from target props like `aoe_damage`); emit one mutation per target.
  </behavior>
  <action>
Author each mechanic module following the pattern from 04-06. For each, ensure:
- Class docstring with Preconditions + Side effects
- `check()` returns CheckResult with concrete reasons on fail
- `apply()` returns list[Mutation]
- `tags: list[str] = [...]` declared

If `ctx.spatial.segment_intersections` / `.nearest` / `.within` exist on MechanicContext (lazy rtree per Phase 3), use them. If not (some ARE listed as GAP-GRAPH01-03 in GAP-ANALYSIS but might be implemented in Phase 3 already — check `src/token_world/graph/spatial.py` methods FIRST), write a straightforward fallback. Document which path was taken in module docstring.

For each mechanic, create `tests/test_mechanic/test_seeds/test_<id>.py` with ≥3 tests covering: happy path (UC scenario), precondition-fail path, and one edge case (e.g., no matching filter / actor missing position).

Use `validate-mechanic` CLI on each new module to pre-flight before committing.
  </action>
  <acceptance_criteria>
    - `src/token_world/mechanic/seeds/look.py`, `find_nearest.py`, `aoe.py` exist
    - Each has `id = "<name>"` and `tags: list[str] = [...]` class attrs
    - Each validates: `uv run token-world validate-mechanic src/token_world/mechanic/seeds/<name>.py` exits 0
    - Each has a test file with ≥3 tests; all pass
  </acceptance_criteria>
  <verify>
    <automated>for m in look find_nearest aoe; do uv run token-world validate-mechanic src/token_world/mechanic/seeds/$m.py || exit 1; done && uv run pytest tests/test_mechanic/test_seeds/test_look.py tests/test_mechanic/test_seeds/test_find_nearest.py tests/test_mechanic/test_seeds/test_aoe.py -x -q</automated>
  </verify>
  <done>
    3 spatial-query mechanics validate + tested.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author speak (MECH13) + try_door (MECH27) + _try_unlock_with_key helper</name>
  <read_first>
    - .planning/use-cases/social/UC-O08-speech-broadcast.md
    - .planning/use-cases/edge-case/UC-E06-move-into-locked-room.md
    - .planning/GAP-ANALYSIS.md §GAP-MECH13, GAP-MECH27, GAP-CROSS02 (refusal narrative — partially absorbed here)
    - src/token_world/mechanic/seeds/_helpers.py (post-04-06; has _find_open_passage)
    - src/token_world/mechanic/seeds/passage_move.py
    - docs/guides/authoring-mechanics.md
  </read_first>
  <behavior>
    speak:
    - id="speak"; voluntary=True; tags=["social","speech","spatial"]
    - check: actor has position; actor has utterance via ctx.target (which is the utterance string) OR reads `utterance` prop on actor
    - apply: find agents within `earshot_radius=15` (or read from actor.voice_volume mapping: whisper=3, talk=10, shout=30); for each, ctx.mutate(hearer, "last_heard_utterances", append-like pattern). Since properties must be JSON-serializable and we can't "append" via mutate without read-modify-write, emit one mutation per hearer that sets `last_heard` list by reading current + appending.
    - Fall back to direct room neighbors if spatial API unavailable.

    try_door:
    - id="try_door"; voluntary=True; tags=["spatial","interaction","passage"]
    - check: actor exists, target is a doorway entity (subtype=doorway)
    - apply: if target.locked==False → no mutation (door is already open-able); if target.locked==True and actor holds an entity with `key_id` matching target.key_id → emit mutation setting target.locked=False; else no mutation — refuse with narrative (write to actor.last_refusal_narrative="the door is locked" or similar, establishing the refusal pattern that 04-08 MECH16 also uses).

    _try_unlock_with_key helper in _helpers.py:
    - `_find_matching_key(ctx, actor, key_id) -> str | None` — walks actor's `holds` edges, returns the id of a held entity whose `key_id` matches the target's required key
  </behavior>
  <action>
Step A — Extend `src/token_world/mechanic/seeds/_helpers.py` with `_find_matching_key`:
```python
def _find_matching_key(ctx: "MechanicContext", actor: str, required_key_id: str) -> str | None:
    """Return id of a held entity whose `key_id` matches, or None."""
    for held in ctx.neighbors(actor, relation="holds"):
        props = ctx.query_node(held)
        if props.get("key_id") == required_key_id:
            return held
    return None
```

Step B — Create `src/token_world/mechanic/seeds/speak.py` and `src/token_world/mechanic/seeds/try_door.py`. For try_door, import `_find_matching_key` from `_helpers`. Use `_find_open_passage` from 04-06 if applicable for spatial context.

Step C — Create dedicated test files with ≥3 tests each:
- test_speak.py: quiet whisper heard only by adjacent agent; shout heard by all in large radius; no agents in earshot → no mutations.
- test_try_door.py: unlocked door → no mutation; locked door + matching key → locked flipped to False; locked door without key → no mutation + actor gets last_refusal_narrative.
  </action>
  <acceptance_criteria>
    - `src/token_world/mechanic/seeds/speak.py` + `try_door.py` exist and validate
    - `grep -n "def _find_matching_key" src/token_world/mechanic/seeds/_helpers.py` returns exactly one match
    - `grep -n "last_refusal_narrative\\|narrative" src/token_world/mechanic/seeds/try_door.py` returns at least one match (refusal pattern)
    - `tests/test_mechanic/test_seeds/test_speak.py` has ≥3 tests
    - `tests/test_mechanic/test_seeds/test_try_door.py` has ≥3 tests
  </acceptance_criteria>
  <verify>
    <automated>for m in speak try_door; do uv run token-world validate-mechanic src/token_world/mechanic/seeds/$m.py || exit 1; done && uv run pytest tests/test_mechanic/test_seeds/test_speak.py tests/test_mechanic/test_seeds/test_try_door.py -x -q</automated>
  </verify>
  <done>
    2 mechanics + helper shipped; refusal narrative pattern established.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip UC-S02, UC-S03, UC-S04, UC-O08, UC-E06 to pass + harness check + VALIDATION.md</name>
  <read_first>
    - Each UC manifest (setup + expected assertions)
    - tests/test_integration/test_use_cases.py
  </read_first>
  <action>
Step A — For each of UC-S02, UC-S03, UC-S04, UC-O08, UC-E06: change `expected_outcome: yield` → `expected_outcome: pass`. Verify graph_assertions in each manifest actually hold after the mechanic runs — if assertions are incomplete or mismatched, augment by editing the manifest's `graph_assertions` block (do NOT change setup or actions — those define the test scenario).

Step B — Run harness targeted:
```bash
uv run pytest tests/test_integration/test_use_cases.py -k "UC-S02 or UC-S03 or UC-S04 or UC-O08 or UC-E06" -q
```
Fix issues: if a UC fails because the harness's verb→mechanic matcher doesn't route `look` to the `look` mechanic (etc.), extend the matcher per 04-06-SUMMARY's noted extension.

Step C — Run full suite and append VALIDATION.md row:

| 04-07-T1 | 04-07 | 4 | MECH-03 | — | MECH02/03/04 spatial-query mechanics validate + pass tests | unit | `uv run pytest tests/test_mechanic/test_seeds/test_look.py tests/test_mechanic/test_seeds/test_find_nearest.py tests/test_mechanic/test_seeds/test_aoe.py -x -q` | ✓ | ⬜ pending |
| 04-07-T2 | 04-07 | 4 | MECH-03 | — | MECH13/27 speak + try_door validate + refusal-narrative pattern | unit | `uv run pytest tests/test_mechanic/test_seeds/test_speak.py tests/test_mechanic/test_seeds/test_try_door.py -x -q` | ✓ | ⬜ pending |
| 04-07-T3 | 04-07 | 4 | MECH-03, TEST-02 | — | UC-S02/S03/S04/O08/E06 flip to pass | integration | `uv run pytest tests/test_integration/test_use_cases.py -k "UC-S02 or UC-S03 or UC-S04 or UC-O08 or UC-E06" -x -q` | ✓ | ⬜ pending |
  </action>
  <acceptance_criteria>
    - 5 UCs grep-checked for `expected_outcome: pass`
    - `uv run pytest tests/test_integration/test_use_cases.py -k "UC-S02 or UC-S03 or UC-S04 or UC-O08 or UC-E06" -q` reports 5 passed
    - `uv run pytest -x -q` exits 0
    - `grep -c "04-07-T" .planning/phases/04-llm-mechanic-generation/04-VALIDATION.md` returns ≥3
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_integration/test_use_cases.py -k "UC-S02 or UC-S03 or UC-S04 or UC-O08 or UC-E06" -q | grep -E "5 passed" && uv run pytest -x -q</automated>
  </verify>
  <done>
    5 mechanics shipped; 5 UCs pass; harness + full suite green.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-04-AST-BYPASS | Tampering | All new mechanic modules | accept | Validation pipeline runs on each; no new attack surface. |
</threat_model>

<verification>
- 5 new mechanics validate
- 5 UCs flip to pass
- Full pytest green
</verification>

<success_criteria>
- GAP-MECH02/03/04/13/27 closed
- Refusal-narrative pattern established in try_door (for 04-08 MECH16 to reuse)
</success_criteria>

<output>
Create `.planning/phases/04-llm-mechanic-generation/04-07-SUMMARY.md` listing: mechanic ids, helper additions, harness matcher changes (if any), UCs flipped.
</output>
