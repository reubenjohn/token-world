---
phase: 06-resident-agent-end-to-end-loop
plan: 01
title: "ResidentAgent module: personality, memory, sessions, agent-turn CLI"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/token_world/resident/__init__.py
  - src/token_world/resident/personality.py
  - src/token_world/resident/memory.py
  - src/token_world/resident/session.py
  - src/token_world/resident/agent.py
  - src/token_world/cli.py
  - tests/test_resident/__init__.py
  - tests/test_resident/conftest.py
  - tests/test_resident/test_personality.py
  - tests/test_resident/test_memory.py
  - tests/test_resident/test_session.py
  - tests/test_resident/test_agent.py
  - tests/test_resident/test_cli_agent_turn.py
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
decisions:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-20
  - D-21
  - D-22
  - D-25
  - D-26
  - D-27
  - D-29
must_haves:
  truths:
    - "src/token_world/resident/ package exists with __init__.py exporting ResidentAgent, AgentMemory, SessionManager, PersonalityGenerator, PersonalityBundle"
    - "PersonalityBundle is a Pydantic BaseModel with fields: name: str, archetype: str, traits: list[str] (3-5), backstory: str, speech_style: str (D-03)"
    - "PersonalityGenerator.generate(universe_description: str, client) -> PersonalityBundle makes one Sonnet call and returns a validated bundle (D-03, D-26)"
    - "AgentMemory class wraps raw sqlite3 access to universe.db: creates agent_memory and agent_sessions tables lazily on first use (D-05, D-06)"
    - "agent_memory table has columns (id, agent_id, session_id, turn_number, action_text, observation_text, timestamp_iso, tick_id) with idx_memory_session(session_id, turn_number) + idx_memory_agent(agent_id, session_id) (D-05)"
    - "agent_sessions table has columns (session_id PRIMARY KEY, agent_id, started_at, forked_from_session_id NULLABLE, snapshot_id INTEGER NULLABLE, memory_summary TEXT NULLABLE, agent_personality TEXT NULLABLE) (D-06)"
    - "AgentMemory.store_turn(agent_id, session_id, turn_number, action, observation, tick_id) inserts one row using parameterized SQL (no ORM)"
    - "AgentMemory.get_context(session_id, window=10) -> tuple[list[tuple[action,observation]], memory_summary: str] returns the last N turns in chronological order + the persisted summary"
    - "AgentMemory.maybe_compact_summary(session_id, client) is called after every store_turn; if total turns in session is a multiple of 10, it calls Haiku to regenerate agent_sessions.memory_summary (D-07, D-27)"
    - "SessionManager.create_session(agent_id, personality) -> session_id inserts agent_sessions row with snapshot_id=None, forked_from_session_id=None, agent_personality=personality.model_dump_json(), returns a new UUID"
    - "SessionManager.fork_session(parent_session_id, graph) -> new_session_id calls graph.snapshot(graph.current_tick, summary=f'fork from {parent}') then inserts a new agent_sessions row with forked_from_session_id=parent_session_id and snapshot_id=<returned id> (D-08)"
    - "SessionManager.restore_session(session_id, graph) calls graph.restore(snapshot_id) if the session has a snapshot_id; otherwise no-op (D-08)"
    - "ResidentAgent class has run_turn() -> str that calls anthropic client.messages.create(model=self._model, system=self._system_prompt, messages=<from rolling window>, max_tokens=256) and returns the stripped text of response.content[0].text (D-01, D-21)"
    - "ResidentAgent system prompt is assembled per D-04: universe CLAUDE.md world rules text + personality block + static instruction block (issue actions as short imperatives, be curious, don't break character); system prompt does NOT include history"
    - "ResidentAgent.system_prompt_text() -> str returns the assembled system prompt (used by 06-05 prompt-hash registry for SHA-256 computation)"
    - "ResidentAgent.run_turn() does NOT call SimulationEngine directly; caller is responsible for feeding action_text to engine.run_tick and calling memory.store_turn with the result (D-21)"
    - "ResidentAgent defaults model to 'claude-haiku-4-5'; a universe-config key agent.model overrides it (D-02)"
    - "token-world agent-turn <universe-slug> [--agent-id <id>] CLI command exists on the Click group in cli.py (D-22)"
    - "agent-turn CLI loads the universe, loads or auto-creates an agent (D-29: auto-create with random personality if none exists), runs one turn, feeds action to SimulationEngine.run_tick, stores the result in AgentMemory, prints the observation to stdout; if the tick yields, runs OperatorHarness.handle_yield then resumes the tick"
    - "PersonalityBundle is stored as a JSON-serializable dict property 'personality' on the agent's graph node (via kg.set()), satisfying ALLOWED_PROPERTY_TYPES (D-03, CLAUDE.md property convention)"
  falsehoods:
    - "ResidentAgent does NOT use the Agent SDK (raw anthropic.Anthropic client per D-01; Agent SDK is operator-only)"
    - "ResidentAgent does NOT call SimulationEngine (clean separation per D-21; caller orchestrates)"
    - "AgentMemory does NOT use SQLAlchemy or any ORM (raw sqlite3 only per CLAUDE.md + D-05)"
    - "AgentMemory does NOT use pickle (JSON only, per CLAUDE.md Critical Constraint #7)"
    - "Session forking does NOT copy the universe.db file (uses existing graph.snapshot per D-08)"
    - "Memory tables are NOT migrated — they are created lazily on first use with CREATE TABLE IF NOT EXISTS (follows graph/persistence.py pattern)"
    - "ResidentAgent does NOT include session history inside the system prompt (history goes in messages array; system prompt is hash-stable per D-04, D-14)"
---

<objective>
Deliver the full Resident Agent subsystem: personality generation, SQLite-backed memory and
sessions, graph-snapshot-based forking, the text-action generation loop, and the `token-world
agent-turn` interactive CLI. This plan satisfies AGENT-01..04 end-to-end and provides the agent
surface that Plan 06-04's playtest runner will drive in Wave 3.

Output:
- `src/token_world/resident/` package (5 files: __init__, personality, memory, session, agent)
- `token-world agent-turn` Click command
- `tests/test_resident/` package with per-module tests + the agent-turn CLI integration test

This plan is independent of Wave-0 (06-00 only touches engine.py, not resident code) and of the
other two Wave-1 plans (06-02 touches engine/compressor; 06-03 touches tests/test_regression/).
All three Wave-1 plans can execute in parallel.
</objective>

<context>
Prior deliverables (from PROJECT.md + Phase 1 + Phase 5):
- `KnowledgeGraph.snapshot(tick_id, summary="") -> int` (returns snapshot_id)
- `KnowledgeGraph.restore(snapshot_id)` (restores graph state + current_tick)
- `KnowledgeGraph.current_tick` / `set_tick()` for tick-id management
- `kg.set(node_id, key, value)` mutation with ALLOWED_PROPERTY_TYPES enforcement
- `kg.claim_id(name)` for unique human-readable IDs
- `SimulationEngine.run_tick(action_text, actor) -> TickResult`
- `OperatorHarness.handle_yield(signal)` is **async** — CLI must wrap with `asyncio.run(...)`
- Existing Click group `cli` in `src/token_world/cli.py` — extend, do not restructure
- `UniverseManager().load(slug) -> Path` for universe path resolution
- Universe dir layout: `<universe>/universe.db` (SQLite) + `<universe>/CLAUDE.md` (world rules)
- Existing model pattern: `src/token_world/engine/classifier.py` shows raw Anthropic SDK usage
  (`client.messages.create(model=..., system=..., messages=..., max_tokens=..., ...)`)

Interfaces the executor must use directly (do not explore codebase):

```python
# src/token_world/graph/__init__.py
from token_world.graph import KnowledgeGraph, Mutation
# kg.snapshot(tick_id: int, summary: str = "") -> int   (returns snapshot_id)
# kg.restore(snapshot_id: int) -> None
# kg.set(node_id: str, key: str, value: Any) -> Mutation
# kg.add_node(node_id: str, node_type: str = "entity", **props) -> Mutation
# kg.claim_id(name: str) -> str
# kg.current_tick: int (property)

# src/token_world/engine/engine.py
class TickResult:
    kind: str  # "ok" | "yielded" | "refused"
    tick_id: str
    observation: str | None
    yield_signal: YieldSignal | None
    trace: ExecutionTrace | None
    refusal_reason: str | None
    projected_state: dict | None  # (added by Wave-0 06-00; unused in this plan)

class SimulationEngine:
    def __init__(self, universe_path: Path, *, graph: KnowledgeGraph, anthropic_client: Any,
                 config: EngineConfig | None = None) -> None: ...
    def run_tick(self, action_text: str, actor: str) -> TickResult: ...

# src/token_world/operator/harness.py
class OperatorHarness:
    def __init__(self, universe: Path) -> None: ...
    async def handle_yield(self, signal: YieldSignal) -> OperatorResult: ...

# src/token_world/engine/config.py
@dataclass(frozen=True, slots=True)
class EngineConfig:
    max_chain_depth: int = 10
    classifier_min_confidence: float = 0.6
    universe_seed: int = 0
```

Discretion (D-26, D-27, D-29 locked in this plan):
- **D-26 Personality prompt**: "Generate a unique character personality for a resident of
  {universe_description}. Return JSON matching: {name: string, archetype: string, traits:
  array of 3-5 adjectives, backstory: string (2-3 sentences), speech_style: string}. Make
  traits internally consistent. Do NOT include any prose outside the JSON."
- **D-27 memory_summary prompt**: "Summarize this character's recent experiences as a
  compact first-person narrative. Focus on discoveries, inventory changes, and relationships.
  Include any important facts the character should remember (items found, people met, places
  visited). Be concise (2-4 sentences). Write as if the character is recalling their own
  memories."
- **D-29 auto-create**: yes. If `token-world agent-turn` is called and no agent_sessions
  row exists for the universe, auto-create an agent node in the graph, generate personality
  (Sonnet one-shot), create a fresh session, and proceed.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Personality generation + PersonalityBundle Pydantic model</name>
  <files>
    - src/token_world/resident/__init__.py
    - src/token_world/resident/personality.py
    - tests/test_resident/__init__.py
    - tests/test_resident/conftest.py
    - tests/test_resident/test_personality.py
  </files>
  <behavior>
    Test 1 (RED): `test_personality_bundle_validates_shape` — construct `PersonalityBundle(name="X", archetype="Y", traits=["a","b","c"], backstory="bs", speech_style="ss")` succeeds; constructing with `traits=[]` or `traits=["a","b","c","d","e","f"]` raises `pydantic.ValidationError` (field validator enforces 3-5 items).
    Test 2 (RED): `test_personality_bundle_roundtrips_json` — `model_dump_json()` then `model_validate_json()` yields an equal bundle.
    Test 3 (RED): `test_generator_calls_sonnet_and_parses_json` — mock `client.messages.create` to return JSON matching the schema; assert `PersonalityGenerator.generate("A misty cavern world", client=mock)` returns a bundle with the mocked fields AND the call used model="claude-sonnet-4-5" (verified via `mock.messages.create.call_args.kwargs["model"]`).
    Test 4 (RED): `test_generator_retries_once_on_malformed_json` — mock returns non-JSON on first call, valid JSON on second; generator succeeds. If both calls return non-JSON, `PersonalityGenerator.generate` raises `ValueError` with message containing "personality generation failed".
    Test 5 (RED): `test_generator_extracts_json_from_wrapped_prose` — mock returns text like `"Here's the personality:\n{\"name\": \"X\", ...}\n"`; generator extracts and parses the JSON block (tolerant to surrounding prose, like existing classifier.py pattern).
  </behavior>
  <action>
    1. Create `src/token_world/resident/__init__.py` with a docstring and initial placeholder
       exports (to be populated as later tasks add classes). Initial contents:
       ```python
       """Resident agent subsystem: personality, memory, sessions, and the run-turn loop."""
       from token_world.resident.personality import PersonalityBundle, PersonalityGenerator
       __all__ = ["PersonalityBundle", "PersonalityGenerator"]
       ```
    2. Create `src/token_world/resident/personality.py`:
       - `PersonalityBundle(BaseModel)` with `model_config = ConfigDict(extra="ignore")` and fields:
         `name: str`, `archetype: str`, `traits: list[str]` with a `@field_validator("traits")`
         requiring 3 <= len <= 5, `backstory: str`, `speech_style: str`.
       - `PersonalityGenerator` dataclass with `generate(universe_description: str, client: Any,
         model: str = "claude-sonnet-4-5") -> PersonalityBundle` method.
       - Generator builds the D-26 prompt, calls `client.messages.create(model=model,
         max_tokens=512, messages=[{"role": "user", "content": <prompt>}])`, extracts
         `response.content[0].text`, pulls the first `{...}` JSON substring (use a regex like
         `re.search(r"\{[\s\S]*\}", text)` — same pattern as classifier.py), validates into a
         `PersonalityBundle` via `model_validate_json(...)`.
       - On json/validation error, retry ONCE. If second attempt still fails, raise
         `ValueError("personality generation failed after retry: <last_error>")`.
    3. Create `tests/test_resident/__init__.py` (empty).
    4. Create `tests/test_resident/conftest.py` with a `mock_anthropic_sonnet` fixture — a
       MagicMock with `.messages.create` returning a response-like object whose
       `.content[0].text` equals a configurable JSON string. Reuse the `MockAnthropicClient`
       pattern from `tests/test_engine/conftest.py` (import + use there if shape matches;
       otherwise define a minimal local version).
    5. Create `tests/test_resident/test_personality.py` with the 5 tests above.
    6. Run `uv run pytest tests/test_resident/test_personality.py -x -v` — all pass.
    7. Run `uv run ruff check src/token_world/resident/ tests/test_resident/` and `uv run mypy
       src/token_world/resident/` — clean.
    8. Commit as `feat(06-01): PersonalityBundle + PersonalityGenerator (one-shot Sonnet) [D-03, D-26]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_resident/test_personality.py -x -v</automated>
  </verify>
  <done>
    PersonalityBundle validates 3-5 traits, roundtrips to JSON. Generator calls Sonnet,
    retries once, extracts JSON from prose. All 5 tests pass. Module exports wired through
    resident/__init__.py.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentMemory SQLite adapter (agent_memory + agent_sessions) with rolling window + summary</name>
  <files>
    - src/token_world/resident/memory.py
    - src/token_world/resident/__init__.py
    - tests/test_resident/test_memory.py
  </files>
  <behavior>
    Test 1 (RED): `test_agent_memory_creates_tables_lazily` — given a fresh universe.db,
    instantiating `AgentMemory(db_path)` and calling any method creates agent_memory and
    agent_sessions tables. Verify via `conn.execute("SELECT name FROM sqlite_master WHERE type='table'")`.
    Test 2 (RED): `test_store_turn_persists_row` — call `store_turn(agent_id="alice",
    session_id="s1", turn_number=0, action_text="look", observation_text="you see a room",
    tick_id="1")`; verify the row exists with correct values.
    Test 3 (RED): `test_get_context_returns_rolling_window_in_chronological_order` — store 12
    turns; `get_context("s1", window=10)` returns exactly 10 turns, in chronological order
    (turn 2 first, turn 11 last).
    Test 4 (RED): `test_get_context_returns_memory_summary` — preseed agent_sessions with
    `memory_summary="Alice explored the forest."`; `get_context("s1")` returns that string as
    the summary tuple element.
    Test 5 (RED): `test_maybe_compact_summary_triggers_haiku_every_10_turns` — mock Haiku
    client to return a fixed summary; store 10 turns via `store_turn` + `maybe_compact_summary`;
    verify `agent_sessions.memory_summary` was written and that the mock was called exactly
    once. Store 9 more turns; verify summary was NOT refreshed (only regenerates at multiples
    of 10).
    Test 6 (RED): `test_store_turn_is_parameterized_and_safe_from_injection` — pass
    `action_text="'; DROP TABLE agent_memory; --"` and verify the row is stored verbatim
    (parameterized query; table still exists; no ORM).
    Test 7 (RED): `test_indexes_exist_on_tables` — verify idx_memory_session and
    idx_memory_agent indexes exist via `SELECT name FROM sqlite_master WHERE type='index'`.
  </behavior>
  <action>
    1. Create `src/token_world/resident/memory.py` with `AgentMemory` class:
       - `__init__(self, db_path: Path) -> None`: stores path, does NOT create tables yet.
       - Private `_ensure_tables(self, conn)` method: runs the CREATE TABLE IF NOT EXISTS
         statements for `agent_memory` and `agent_sessions` per D-05, D-06 schema (see
         06-RESEARCH.md §"Memory Persistence" for exact SQL). Also creates both indexes.
         Called at the top of every public method.
       - `store_turn(self, agent_id: str, session_id: str, turn_number: int, action_text: str,
         observation_text: str, tick_id: str) -> None`: opens connection via
         `with sqlite3.connect(str(self._db_path)) as conn:`, calls `_ensure_tables`, inserts
         row with `timestamp_iso=datetime.now(UTC).isoformat()`. Use parameterized `?`
         placeholders.
       - `get_context(self, session_id: str, window: int = 10) -> tuple[list[tuple[str, str]], str]`:
         See 06-RESEARCH.md §"AgentMemory schema and key query" for the exact query pattern.
         Returns (turns_list_in_chronological_order, memory_summary_or_empty_string).
       - `maybe_compact_summary(self, session_id: str, client: Any, model: str =
         "claude-haiku-4-5") -> None`: counts rows for session; if count > 0 and count % 10 == 0,
         fetches ALL turns for session, builds the D-27 prompt with those turns, calls Haiku
         (`client.messages.create`), stores the returned text as `agent_sessions.memory_summary`
         via an UPDATE.
    2. Re-export `AgentMemory` from `src/token_world/resident/__init__.py`.
    3. Write 7 tests in `tests/test_resident/test_memory.py` using a `tmp_path / "universe.db"`
       fixture. Use the mock Haiku client from conftest.py.
    4. Run `uv run pytest tests/test_resident/test_memory.py -x -v` — all pass.
    5. Run ruff + mypy — clean.
    6. Commit as `feat(06-01): AgentMemory (agent_memory + agent_sessions) with rolling window [D-05, D-06, D-07, D-27]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_resident/test_memory.py -x -v</automated>
  </verify>
  <done>
    AgentMemory lazy-creates both tables + 2 indexes; store_turn/get_context are parameterized;
    maybe_compact_summary triggers Haiku only at 10-turn boundaries. All 7 tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SessionManager with fork_session via graph snapshot</name>
  <files>
    - src/token_world/resident/session.py
    - src/token_world/resident/__init__.py
    - tests/test_resident/test_session.py
  </files>
  <behavior>
    Test 1 (RED): `test_create_session_inserts_row_with_new_uuid` — `SessionManager(db_path)
    .create_session(agent_id="alice", personality=bundle)` returns a UUID string; the row
    exists with agent_personality = bundle.model_dump_json(), snapshot_id=NULL,
    forked_from_session_id=NULL.
    Test 2 (RED): `test_fork_session_calls_graph_snapshot_and_links` — mock a KnowledgeGraph
    with `snapshot()` returning 42; `fork_session(parent_session_id="s1", graph=kg)` returns a
    new session id; new row has forked_from_session_id="s1" and snapshot_id=42. Verify
    `kg.snapshot` was called with current_tick and a `summary` matching pattern `"fork from s1"`.
    Test 3 (RED): `test_restore_session_calls_graph_restore_when_snapshot_present` — create
    session with snapshot_id=42; `restore_session(session_id, graph=mock_graph)` calls
    `graph.restore(42)`. If snapshot_id is None, `graph.restore` is NOT called.
    Test 4 (RED): `test_get_session_returns_row_as_dict` — `get_session(session_id)` returns
    `{session_id, agent_id, started_at, forked_from_session_id, snapshot_id, memory_summary,
    agent_personality}` dict. Returns None if not found.
    Test 5 (RED): `test_list_sessions_for_agent_returns_all` — create 3 sessions for agent
    "alice", 1 for agent "bob"; `list_sessions("alice")` returns 3 session IDs.
    Test 6 (integration RED): `test_fork_from_real_graph_preserves_state` — create a real
    KnowledgeGraph with a db-backed path, add a node, create session, mutate graph, fork
    session, mutate again, restore fork → the node returned to the fork-point state. Use
    kg fixture with SQLite path.
  </behavior>
  <action>
    1. Create `src/token_world/resident/session.py` with `SessionManager` class:
       - `__init__(self, db_path: Path) -> None`.
       - Uses the same `_ensure_tables` lazy-init pattern as AgentMemory (factor to a shared
         helper function `_ensure_memory_tables(conn)` in `memory.py` that BOTH classes
         import, to avoid duplicating the DDL).
       - `create_session(agent_id: str, personality: PersonalityBundle) -> str`: insert row
         with fresh `uuid.uuid4()` session_id, `started_at=datetime.now(UTC).isoformat()`,
         snapshot_id=None, forked_from_session_id=None,
         agent_personality=personality.model_dump_json(), memory_summary=None.
       - `fork_session(parent_session_id: str, graph: KnowledgeGraph) -> str`: verifies
         parent exists (raise KeyError if not); calls
         `snapshot_id = graph.snapshot(graph.current_tick, summary=f"fork from {parent_session_id}")`;
         fetches parent's agent_id and agent_personality; inserts new session row with
         `forked_from_session_id=parent_session_id`, `snapshot_id=snapshot_id`, same
         agent_id/agent_personality as parent (fork inherits identity).
       - `restore_session(session_id: str, graph: KnowledgeGraph) -> None`: fetch session; if
         snapshot_id is not None, call `graph.restore(snapshot_id)`; if None, no-op (session
         starts fresh).
       - `get_session(session_id: str) -> dict | None`: single row fetch, keyed return.
       - `list_sessions(agent_id: str) -> list[str]`: list session_ids for agent, chronological.
    2. Re-export `SessionManager` from `src/token_world/resident/__init__.py`.
    3. Write 6 tests (the last one uses the kg fixture for real-graph integration). The kg
       fixture pattern with SQLite is visible in `tests/test_engine/test_engine_run_tick.py`
       conftest.
    4. Run tests, ruff, mypy — clean.
    5. Commit as `feat(06-01): SessionManager fork + restore via graph snapshot [D-06, D-08]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_resident/test_session.py -x -v</automated>
  </verify>
  <done>
    SessionManager.create/fork/restore/get/list all work against real universe.db; fork_session
    calls graph.snapshot with correct args; restore_session delegates to graph.restore. 6 tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: ResidentAgent (raw Anthropic SDK loop) with system-prompt assembly</name>
  <files>
    - src/token_world/resident/agent.py
    - src/token_world/resident/__init__.py
    - tests/test_resident/test_agent.py
  </files>
  <behavior>
    Test 1 (RED): `test_build_system_prompt_includes_personality_and_rules` — construct agent
    with world_rules="RULES" and a personality bundle; `system_prompt_text()` contains "RULES",
    the personality name, archetype, all traits, backstory, and speech_style. Also contains
    the static instruction block "Issue actions as short imperative sentences". Does NOT
    contain any turn history.
    Test 2 (RED): `test_run_turn_calls_haiku_and_returns_stripped_text` — mock client returns
    response whose `.content[0].text == "  look around  \n"`; `run_turn()` returns "look around"
    (stripped). Verify `client.messages.create` was called with `model="claude-haiku-4-5"`
    (default) and non-empty system prompt and a messages list.
    Test 3 (RED): `test_run_turn_uses_rolling_context_from_memory` — preseed AgentMemory with
    5 turns (actions "a1".."a5", observations "o1".."o5"); `run_turn()` constructs a messages
    array where user turns contain each action text and assistant turns contain each
    observation, in chronological order, ending with a user prompt asking for the next action.
    Test 4 (RED): `test_run_turn_includes_memory_summary_when_present` — preseed session with
    memory_summary="You found a key earlier."; run_turn includes that summary in the first
    user message (e.g., prepended as "Memory: {summary}\n\n").
    Test 5 (RED): `test_run_turn_respects_configured_model` — construct agent with
    `model="claude-sonnet-4-5"`; verify `client.messages.create` is called with that model
    (D-02 override path).
    Test 6 (RED): `test_agent_personality_stored_as_dict_property_on_graph_node` — test for a
    factory helper `create_agent_node(graph, agent_id, personality)` that calls
    `graph.add_node(agent_id, node_type="agent")` and `graph.set(agent_id, "personality",
    personality.model_dump())`. After the call, `graph.query_node(agent_id)` returns a dict
    containing `personality` with the expected shape (dict, not str).
  </behavior>
  <action>
    1. Create `src/token_world/resident/agent.py` with:
       - `ResidentAgent` class per 06-RESEARCH.md §"ResidentAgent.run_turn() full pattern".
       - `__init__(self, agent_id, session_id, personality: PersonalityBundle, memory:
         AgentMemory, client: Any, *, model: str = "claude-haiku-4-5", world_rules: str = "")`.
       - Private `_build_system_prompt()` method using 06-RESEARCH.md §"System Prompt Assembly"
         exact template.
       - Public `system_prompt_text(self) -> str` returns `self._system_prompt` (so Plan 06-05
         can hash it for D-14).
       - `_build_messages(self) -> list[dict]`: fetch `(turns, summary)` via
         `self._memory.get_context(self._session_id, window=10)`; build alternating user/
         assistant turns; if summary is non-empty, prepend "Memory (older events): {summary}\n\n"
         to the first user message; append a final user message like "What do you do next?".
       - `run_turn(self) -> str`: build messages; call
         `self._client.messages.create(model=self._model, system=self._system_prompt,
         messages=messages, max_tokens=256)`; return `response.content[0].text.strip()`.
       - Module-level helper `create_agent_node(graph, agent_id, personality) -> None`:
         `graph.add_node(agent_id, node_type="agent")` then
         `graph.set(agent_id, "personality", personality.model_dump())`. Treat as plan-scoped
         public helper (re-exported from __init__).
    2. Re-export `ResidentAgent` and `create_agent_node` from `resident/__init__.py`.
    3. Write 6 tests. For Test 6, use the `kg` fixture from test_graph/conftest.py
       (instantiate KnowledgeGraph, no SQLite path needed for graph-only assertion).
    4. Run tests, ruff, mypy — clean.
    5. Commit as `feat(06-01): ResidentAgent raw Anthropic loop [D-01, D-02, D-04, D-21]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_resident/test_agent.py -x -v</automated>
  </verify>
  <done>
    ResidentAgent constructs hash-stable system prompt (no history), builds alternating
    message context, respects configured model, personalities are attached as dict graph
    properties. 6 tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: token-world agent-turn CLI command with auto-create</name>
  <files>
    - src/token_world/cli.py
    - tests/test_resident/test_cli_agent_turn.py
  </files>
  <behavior>
    Test 1 (RED, unit with mocks): `test_agent_turn_loads_universe_or_errors` — CLI called
    with a nonexistent slug exits non-zero and writes error to stderr.
    Test 2 (RED, unit with mocks): `test_agent_turn_auto_creates_agent_when_none_exists` —
    clean universe with no agent_sessions rows; `agent-turn <slug>` calls the
    PersonalityGenerator (Sonnet mocked) and SessionManager.create_session, then proceeds
    with one turn. Exit 0. Graph has the new agent node with a personality property.
    Test 3 (RED, unit with mocks): `test_agent_turn_runs_one_tick_and_prints_observation` —
    Mock ResidentAgent.run_turn to return "look around"; mock SimulationEngine.run_tick to
    return TickResult.ok(observation="you see the room", ...); invoke CLI; stdout contains
    "you see the room".
    Test 4 (RED, unit with mocks): `test_agent_turn_handles_yield_via_operator_harness` —
    Mock run_tick to return TickResult.yielded(...) first time, TickResult.ok(...) second;
    mock OperatorHarness.handle_yield; assert harness called once; final observation printed.
    Test 5 (RED, unit with mocks): `test_agent_turn_persists_turn_to_memory` — after invoke,
    agent_memory table has one row with turn_number=0, action="look around", observation=
    "you see the room".
  </behavior>
  <action>
    1. Extend `src/token_world/cli.py`:
       - Add module-level imports (keep existing import style):
         ```python
         from token_world.resident import (
             AgentMemory, PersonalityGenerator, ResidentAgent,
             SessionManager, create_agent_node,
         )
         from token_world.engine import SimulationEngine
         import anthropic
         ```
       - Add new command:
         ```python
         @cli.command("agent-turn")
         @click.argument("slug")
         @click.option("--agent-id", default=None, help="Existing agent id; auto-created if omitted and no agent exists")
         @click.option("--no-operator", is_flag=True, help="Do not invoke OperatorHarness on yield")
         def agent_turn(slug: str, agent_id: str | None, no_operator: bool) -> None:
             """Run one agent turn interactively: agent → engine → print observation."""
             ...
         ```
       - Logic flow (in order):
         (a) `UniverseManager().load(slug)` → universe_dir (on FileNotFoundError → click.echo
             error, `raise SystemExit(1) from e`).
         (b) Load graph: `kg = KnowledgeGraph(db_path=universe_dir / "universe.db"); kg.load()`.
         (c) Load world rules: read `universe_dir / "CLAUDE.md"` as text (fallback to empty
             string if missing).
         (d) Construct `client = anthropic.Anthropic()`.
         (e) Construct `memory = AgentMemory(universe_dir / "universe.db")`.
         (f) Construct `sessions = SessionManager(universe_dir / "universe.db")`.
         (g) If `agent_id is None`:
             - List agents via `[s["agent_id"] for s in sessions.list_sessions_all()]` (add a
               minimal `list_agents() -> list[str]` method to SessionManager to support this;
               or query directly here: `SELECT DISTINCT agent_id FROM agent_sessions`).
             - If list is empty → auto-create: generate personality via
               `PersonalityGenerator().generate(universe_description=<world_rules first line>,
               client=client)`, claim an id via `kg.claim_id("resident")`, call
               `create_agent_node(kg, new_id, personality)`, `sessions.create_session(new_id,
               personality)`. Use that id + new session id.
             - If list is non-empty → pick first agent_id; find latest session for that agent
               via list_sessions; resume it (no restore needed unless explicit).
         (h) Else: fetch existing sessions for `agent_id`, pick the most recent.
         (i) Fetch the session's personality from agent_sessions.agent_personality (JSON) and
             validate into a PersonalityBundle. If none (fresh agent), use the one generated.
         (j) Construct `ResidentAgent(agent_id=..., session_id=..., personality=bundle,
             memory=memory, client=client, world_rules=world_rules)`.
         (k) Call `action = agent.run_turn()`.
         (l) Construct `engine = SimulationEngine(universe_dir, graph=kg,
             anthropic_client=client)`.
         (m) Call `result = engine.run_tick(action, actor=agent_id)`.
         (n) If `result.kind == "yielded"` and not `no_operator`:
             - `harness = OperatorHarness(universe_dir)`
             - `asyncio.run(harness.handle_yield(result.yield_signal))`
             - `result = engine.run_tick(action, actor=agent_id)` (resume).
         (o) Write to memory:
             `turn_number = sessions.get_next_turn_number(session_id)` (add this helper to
             SessionManager: returns max(turn_number)+1 from agent_memory for session, else 0).
             `memory.store_turn(agent_id, session_id, turn_number, action,
             result.observation or "", result.tick_id)`.
             `memory.maybe_compact_summary(session_id, client)`.
         (p) Save graph state: `kg.save()`.
         (q) Print via click.echo: `Agent ({agent_id}): {action}\n\nObservation:\n{result.observation}`.
         (r) If result.kind == "refused", print `(refused: {result.refusal_reason})` after obs.
    2. Do NOT change any existing Click commands.
    3. Write 5 tests using Click's `CliRunner` and extensive mocking. Use monkeypatch to
       patch `anthropic.Anthropic`, `SimulationEngine.run_tick`, `OperatorHarness.handle_yield`,
       `PersonalityGenerator.generate` where needed. Patch `asyncio.run` to return a
       MagicMock when exercising yield paths.
    4. Run tests, ruff, mypy (on cli.py where feasible — it has mixed third-party interfaces
       so `# type: ignore[...]` is acceptable for anthropic.Anthropic() instantiation).
    5. Commit as `feat(06-01): token-world agent-turn CLI command [D-22, D-29]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_resident/test_cli_agent_turn.py -x -v</automated>
  </verify>
  <done>
    `token-world agent-turn <slug>` runs one full turn end-to-end with mocked LLM; handles
    yield via OperatorHarness; persists to memory; prints observation. 5 tests pass. Existing
    `token-world` commands unchanged.
  </done>
</task>

</tasks>

<risks>
- **File overlap with 06-04**: both plans modify `src/token_world/cli.py`. 06-04 is in Wave 3,
  06-01 in Wave 1 — sequential, no conflict. Flagged for awareness.
- **Personality JSON extraction brittleness**: Sonnet may wrap JSON in markdown fences. The
  regex-based extractor handles both fenced and inline JSON (see classifier.py for the pattern).
  The retry-once pattern mitigates most transient issues.
- **anthropic.Anthropic() initialization**: instantiates on env var `ANTHROPIC_API_KEY`. In
  unit tests, patch the constructor at the cli.py module level.
- **`agent-turn` mixed concerns**: this command does a lot (load, auto-create, run, persist).
  Keep logic flat and well-commented; resist the temptation to refactor into a class — the
  next plan (06-04 playtest runner) will extract the shared patterns into a reusable loop.
- **Memory compaction cost**: every 10 turns, a Haiku call runs. That's fine for interactive
  use. Scorer (06-05) does not use memory, so it remains O(1) per turn.
- **Graph ownership on agent creation**: calling `create_agent_node` inside `agent-turn`
  mutates the graph. The `kg.save()` at step (p) persists. If the tick failed before (m),
  we still save the agent creation — which is desired (agent exists now).
- **`OperatorHarness.handle_yield` is async**: asyncio.run must not be inside an existing
  event loop. In the synchronous CLI entry point this is safe. In tests, we mock the harness
  and patch asyncio.run.
</risks>

<output>
After completion, create `.planning/phases/06-resident-agent-end-to-end-loop/06-01-SUMMARY.md`
documenting: (a) resident/ module layout with exports, (b) schema of agent_memory and
agent_sessions tables (DDL block), (c) the D-26 personality prompt and D-27 memory summary
prompt chosen, (d) the agent-turn command's flow diagram (load → auto-create → run_turn →
run_tick → yield-handler → store → print), (e) how Plan 06-04 will reuse ResidentAgent +
AgentMemory + SessionManager from the playtest runner.
</output>
