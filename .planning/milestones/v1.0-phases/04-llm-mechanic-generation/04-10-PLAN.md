---
phase: 04-llm-mechanic-generation
plan: 10
title: "Seed cluster: resource/durability (MECH17 degrade, MECH18 fungible_pay)"
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-08
files_modified:
  - src/token_world/mechanic/seeds/degrade.py
  - src/token_world/mechanic/seeds/fungible_pay.py
  - tests/test_mechanic/test_seeds/test_degrade.py
  - tests/test_mechanic/test_seeds/test_fungible_pay.py
  - .planning/use-cases/resource/UC-R05-degradation-over-time.md
  - .planning/use-cases/resource/UC-R06-fungible-currency.md
  - .planning/phases/04-llm-mechanic-generation/04-VALIDATION.md
autonomous: true
requirements:
  - MECH-03
gap_ids:
  - GAP-MECH17
  - GAP-MECH18
decisions:
  - D-03
  - D-11
  - D-37
threats:
  - T-04-AST-BYPASS
must_haves:
  truths:
    - "degrade mechanic decrements durability when a tool is used; removes the node when durability <= 0"
    - "fungible_pay mechanic subset-sums held fungible-currency entities to a target amount and transfers them (no change-making — GAP-MECH29 deferred)"
    - "UC-R05 expected_outcome updated: ambient-decay form remains `blocked` (GAP-ENG07 passive-tick sweep, Phase 5); use-on-tool form flips to `pass` if the manifest targets that action. If the manifest strictly tests ambient decay, keep blocked."
    - "UC-R06 flips to pass (single-tick subset-sum)"
  artifacts:
    - path: "src/token_world/mechanic/seeds/degrade.py"
      provides: "MECH17 wear/durability on tool use"
    - path: "src/token_world/mechanic/seeds/fungible_pay.py"
      provides: "MECH18 subset-sum fungible currency transfer"
---

<objective>
Ship 2 resource seed mechanics. Flip UC-R06; update UC-R05 outcome per decision tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-llm-mechanic-generation/04-CONTEXT.md
@.planning/GAP-ANALYSIS.md
@.planning/use-cases/resource/UC-R05-degradation-over-time.md
@.planning/use-cases/resource/UC-R06-fungible-currency.md
@docs/guides/authoring-mechanics.md
@src/token_world/mechanic/seeds/_helpers.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author MECH17 degrade + MECH18 fungible_pay + tests</name>
  <read_first>
    - .planning/use-cases/resource/UC-R05-degradation-over-time.md
    - .planning/use-cases/resource/UC-R06-fungible-currency.md
    - .planning/GAP-ANALYSIS.md §GAP-MECH17, GAP-MECH18, GAP-MECH29 (change-making — deferred)
    - src/token_world/mechanic/seeds/_helpers.py
    - docs/guides/authoring-mechanics.md
  </read_first>
  <behavior>
    degrade:
    - id="degrade"; voluntary=True (use-on-tool form); tags=["resource","durability"]
    - check: actor holds target; target has `durability` property (int > 0)
    - apply: decrement durability by 1 (or by `usage_cost` prop); if resulting durability <= 0 → remove_node(target) AND remove the holds edge; else just set durability
    - Ambient decay (periodic tick-sweep) is GAP-ENG07, Phase 5's concern; NOT in this mechanic.

    fungible_pay:
    - id="fungible_pay"; voluntary=True; tags=["resource","currency"]
    - check: actor has held coin-entities (subtype=coin or matching `fungible_kind`); actor has `pending_payment = {"recipient": <node>, "amount": N, "kind": "coin"}`; a subset of held coins sums exactly to N (no change-making)
    - apply: for the chosen subset, transfer each coin's `holds` edge from actor to recipient
    - If no exact subset exists, refuse with reason "cannot make exact change (GAP-MECH29 deferred)"
  </behavior>
  <action>
Author both mechanics per the baseline pattern. For `fungible_pay`'s subset-sum, implement a simple exponential backtracking search (N held coins ≤ ~20 typical; performance adequate). For a helper, consider `_subset_sum(values, target) -> list[int] | None` in `_helpers.py` — only add if MECH18's tests exercise ≥2 scenarios.

Each mechanic: ≥3 tests. For fungible_pay: exact-subset happy path; no-subset-sum → refuse with change reason; single-coin-exact match; pending_payment missing → refuse.
  </action>
  <acceptance_criteria>
    - `src/token_world/mechanic/seeds/degrade.py` + `fungible_pay.py` exist and validate
    - `grep -n "GAP-MECH29\\|cannot make exact change\\|no exact subset" src/token_world/mechanic/seeds/fungible_pay.py` returns at least one match (deferred concern documented)
    - Tests: ≥3 per mechanic; all pass
  </acceptance_criteria>
  <verify>
    <automated>for m in degrade fungible_pay; do uv run token-world validate-mechanic src/token_world/mechanic/seeds/$m.py || exit 1; done && uv run pytest tests/test_mechanic/test_seeds/test_degrade.py tests/test_mechanic/test_seeds/test_fungible_pay.py -x -q</automated>
  </verify>
  <done>
    2 mechanics shipped + tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update UC-R05 and UC-R06 expected_outcome + VALIDATION.md + full suite</name>
  <read_first>
    - .planning/use-cases/resource/UC-R05-degradation-over-time.md (decide whether its actions hit the use-on-tool path or the ambient-decay path)
    - .planning/use-cases/resource/UC-R06-fungible-currency.md
    - tests/test_integration/test_use_cases.py
  </read_first>
  <action>
Step A — Read UC-R05. If `actions` include a "use" / "swing" / "hit" verb that targets a tool with durability → `expected_outcome: pass`. If `actions` are purely time-lapse / tick-based with no actor verb → `expected_outcome: blocked` with reason `GAP-ENG07 passive-tick sweep`. Document decision in 04-10-SUMMARY.md. Matcher/harness behavior (including any tag-based routing for stubs) is owned by 04-04 — see 04-04-SUMMARY Extension Contract and `tests/test_mechanic/test_harness_matcher.py`. Do NOT extend it from this plan.

Step B — Flip UC-R06 to `expected_outcome: pass`.

Step C — Run harness targeted + full suite:
```bash
uv run pytest tests/test_integration/test_use_cases.py -k "UC-R05 or UC-R06" -q
uv run pytest -x -q
```

Step D — Append to VALIDATION.md:

| 04-10-T1 | 04-10 | 4 | MECH-03 | — | MECH17/18 degrade + fungible_pay validate + tests | unit | `uv run pytest tests/test_mechanic/test_seeds/test_degrade.py tests/test_mechanic/test_seeds/test_fungible_pay.py -x -q` | ✓ | ⬜ pending |
| 04-10-T2 | 04-10 | 4 | MECH-03, TEST-02 | — | UC-R06 flips to pass; UC-R05 status updated per decision tree | integration | `uv run pytest tests/test_integration/test_use_cases.py -k "UC-R05 or UC-R06" -q` | ✓ | ⬜ pending |
  </action>
  <acceptance_criteria>
    - UC-R06 grep-checked for `expected_outcome: pass`
    - UC-R05 has either `expected_outcome: pass` OR `expected_outcome: blocked` (with rationale in 04-10-SUMMARY.md)
    - `uv run pytest tests/test_integration/test_use_cases.py -k "UC-R06" -q` reports 1 passed
    - `uv run pytest -x -q` exits 0
    - `grep -c "04-10-T" .planning/phases/04-llm-mechanic-generation/04-VALIDATION.md` returns ≥2
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_integration/test_use_cases.py -k "UC-R06" -q | grep -E "1 passed" && uv run pytest -x -q</automated>
  </verify>
  <done>
    2 mechanics + harness updates; full suite green.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-04-AST-BYPASS | Tampering | New mechanics | accept | Validation pipeline runs on each. |
</threat_model>

<verification>
- Both mechanics validate; UC-R06 passes; UC-R05 outcome-classified; full pytest green
</verification>

<success_criteria>
- GAP-MECH17, GAP-MECH18 closed
- UC-R05 status documented (pass OR blocked depending on manifest action shape)
- GAP-MECH29 (change-making) properly deferred in fungible_pay docstring
</success_criteria>

<output>
Create `.planning/phases/04-llm-mechanic-generation/04-10-SUMMARY.md` listing: mechanic ids, UC-R05 decision + rationale, UC-R06 flipped.
</output>
