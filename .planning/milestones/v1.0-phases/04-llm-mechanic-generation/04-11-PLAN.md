---
phase: 04-llm-mechanic-generation
plan: 11
title: "Seed cluster: environmental family (MECH20 fire_spread, MECH21 weather_reaction, MECH22 decay_tick, MECH23 illumination, MECH24 contagion)"
type: execute
wave: 4
depends_on:
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-09
files_modified:
  - src/token_world/mechanic/seeds/fire_spread.py
  - src/token_world/mechanic/seeds/weather_reaction.py
  - src/token_world/mechanic/seeds/decay_tick.py
  - src/token_world/mechanic/seeds/illumination.py
  - src/token_world/mechanic/seeds/contagion.py
  - tests/test_mechanic/test_seeds/test_fire_spread.py
  - tests/test_mechanic/test_seeds/test_weather_reaction.py
  - tests/test_mechanic/test_seeds/test_decay_tick.py
  - tests/test_mechanic/test_seeds/test_illumination.py
  - tests/test_mechanic/test_seeds/test_contagion.py
  - .planning/use-cases/environmental/UC-V01-fire-spread.md
  - .planning/use-cases/environmental/UC-V02-weather-change.md
  - .planning/use-cases/environmental/UC-V03-decay.md
  - .planning/use-cases/environmental/UC-V04-seasons.md
  - .planning/use-cases/environmental/UC-V06-light-and-dark.md
  - .planning/use-cases/environmental/UC-V07-contagion.md
  - .planning/phases/04-llm-mechanic-generation/04-VALIDATION.md
autonomous: true
requirements:
  - MECH-03
gap_ids:
  - GAP-MECH20
  - GAP-MECH21
  - GAP-MECH22
  - GAP-MECH23
  - GAP-MECH24
decisions:
  - D-03
  - D-11
  - D-37
  - D-38
threats:
  - T-04-AST-BYPASS
must_haves:
  truths:
    - "fire_spread: involuntary; watches temperature changes on flammable neighbors (reuses environmental_reaction seed pattern)"
    - "weather_reaction: shipped AS A STUB with class-level blocked_by=\"GAP-ENG09\" per D-38 (decision at authoring time, not at execution). The decision is committed in this plan: the mechanic DOES NOT attempt a real WorldPropertyMatcher implementation; UC-V02/UC-V04 remain blocked until Phase 5 delivers GAP-ENG09. If during execution it turns out GAP-ENG09 is NOT required (e.g., a workaround exists via existing matchers), that is a deviation — handled per execute-phase deviation protocol, not by the plan."
    - "decay_tick: voluntary (Phase 4 wrapper; real tick-end invocation is GAP-ENG07 Phase 5) — applies a one-step rot delta to a target with decay_period"
    - "illumination: involuntary; watches lit/extinguished property changes on light-sources; recomputes room illumination level"
    - "contagion: voluntary (one-step transmission); uses random.random(). `ctx.seed` / `ctx._seed` is a documented gap (GAP-GRAPH05, see `docs/guides/authoring-mechanics.md` § Known gaps); stub accordingly — for Phase 4 tests, seed a local `random.Random(seed=tick_id_or_fallback)` instance and document the determinism gap in the module docstring."
    - "UC-V01, UC-V03, UC-V06, UC-V07 flip to pass (single-step / use-on-trigger forms)"
    - "UC-V02 (weather), UC-V04 (seasons) REMAIN expected_outcome=blocked (set in 04-04); the weather_reaction stub's blocked_by=\"GAP-ENG09\" provides the matching engine-side signal for the harness"
  artifacts:
    - path: "src/token_world/mechanic/seeds/fire_spread.py"
      provides: "MECH20 fire propagation to flammable neighbors"
    - path: "src/token_world/mechanic/seeds/weather_reaction.py"
      provides: "MECH21 stub (blocked_by=GAP-ENG09 — committed at authoring time per D-38)"
    - path: "src/token_world/mechanic/seeds/decay_tick.py"
      provides: "MECH22 one-step decay"
    - path: "src/token_world/mechanic/seeds/illumination.py"
      provides: "MECH23 room illumination propagation"
    - path: "src/token_world/mechanic/seeds/contagion.py"
      provides: "MECH24 probabilistic transmission"
---

<objective>
Ship the environmental mechanic family. Mix of real implementations and targeted framework-gap stubs (weather/seasons depend on GAP-ENG09 `WorldPropertyMatcher` which is Phase 5). Flip 4 environmental UCs to pass; keep 2 (UC-V02, UC-V04) correctly blocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-llm-mechanic-generation/04-CONTEXT.md
@.planning/phases/04-llm-mechanic-generation/04-RESEARCH.md
@.planning/phases/04-llm-mechanic-generation/04-09-SUMMARY.md
@.planning/GAP-ANALYSIS.md
@.planning/use-cases/environmental/UC-V01-fire-spread.md
@.planning/use-cases/environmental/UC-V02-weather-change.md
@.planning/use-cases/environmental/UC-V03-decay.md
@.planning/use-cases/environmental/UC-V04-seasons.md
@.planning/use-cases/environmental/UC-V06-light-and-dark.md
@.planning/use-cases/environmental/UC-V07-contagion.md
@docs/guides/authoring-mechanics.md
@src/token_world/mechanic/seeds/environmental_reaction.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author MECH20 fire_spread + MECH22 decay_tick + MECH23 illumination</name>
  <read_first>
    - .planning/use-cases/environmental/UC-V01-fire-spread.md
    - .planning/use-cases/environmental/UC-V03-decay.md
    - .planning/use-cases/environmental/UC-V06-light-and-dark.md
    - .planning/GAP-ANALYSIS.md §GAP-MECH20/22/23, GAP-ENG07 (passive tick sweep — Phase 5), GAP-ENG08 (cycle detector — Phase 5)
    - src/token_world/mechanic/seeds/environmental_reaction.py (existing seed with involuntary matcher pattern)
    - docs/guides/authoring-mechanics.md §5 Matcher Declaration, §9 Reactive Cycles
  </read_first>
  <behavior>
    fire_spread: involuntary.
    - id="fire_spread"; voluntary=False; tags=["environmental","fire","involuntary"]
    - watches: PropertyChangeMatcher on `temperature` or `on_fire` transitions to True
    - check: target has `on_fire=True`; neighbors include flammable entities (has `flammable=True` or subtype in flammable set)
    - apply: for each flammable neighbor, set on_fire=True (partial — one hop per tick; relies on chain engine for cascade)
    - Guard against reactive cycles (GAP-MECH26): only spreads TO nodes not already on fire

    decay_tick: voluntary (Phase 4 wrapper).
    - id="decay_tick"; voluntary=True; tags=["environmental","decay"]
    - check: target has `decay_period` and `decay_progress` (int); is_rotten=False
    - apply: increment decay_progress by 1; if decay_progress >= decay_period → set `rotten=True` and optionally property_name="state" value="rotten"
    - Ambient invocation (every tick, all decay-eligible nodes) is GAP-ENG07 Phase 5

    illumination: involuntary.
    - id="illumination"; voluntary=False; tags=["environmental","light","involuntary"]
    - watches: PropertyChangeMatcher on `lit` property of torch/lamp entities
    - check: target is a light source that transitioned lit→True or lit→False; room containing it has `illumination` property
    - apply: recompute illumination = sum of `brightness` of all located_in-connected lit sources in room; mutate room.illumination
  </behavior>
  <action>
Author 3 mechanic modules + 3 test files per baseline pattern. Reuse `environmental_reaction.py` as a template for involuntary mechanics (watches() + matchers).

For each involuntary mechanic: tests must include at least one case exercising the matcher — i.e., run the ChainExecutionEngine after a trigger mutation and assert the involuntary fires. Use the Phase 2 engine API.

For `decay_tick`, NOTE in the docstring: "Phase 4 voluntary; Phase 5 (GAP-ENG07) will invoke as a tick-end passive sweep."
  </action>
  <acceptance_criteria>
    - 3 modules + 3 test files exist; all validate
    - `grep -n "voluntary = False" src/token_world/mechanic/seeds/fire_spread.py src/token_world/mechanic/seeds/illumination.py | wc -l` returns 2 (both involuntary)
    - `grep -n "def watches" src/token_world/mechanic/seeds/fire_spread.py src/token_world/mechanic/seeds/illumination.py | wc -l` returns 2
    - Each test file has ≥3 tests; all pass
  </acceptance_criteria>
  <verify>
    <automated>for m in fire_spread decay_tick illumination; do uv run token-world validate-mechanic src/token_world/mechanic/seeds/$m.py || exit 1; done && uv run pytest tests/test_mechanic/test_seeds/test_fire_spread.py tests/test_mechanic/test_seeds/test_decay_tick.py tests/test_mechanic/test_seeds/test_illumination.py -x -q</automated>
  </verify>
  <done>
    3 mechanics shipped + tested.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author MECH24 contagion + MECH21 weather_reaction (stub if framework-gap-blocked)</name>
  <read_first>
    - .planning/use-cases/environmental/UC-V02-weather-change.md
    - .planning/use-cases/environmental/UC-V04-seasons.md
    - .planning/use-cases/environmental/UC-V07-contagion.md
    - .planning/GAP-ANALYSIS.md §GAP-MECH21 (WorldPropertyMatcher — GAP-ENG09, Phase 5), §GAP-MECH24 (seeded RNG — GAP-GRAPH05, Phase 5)
    - src/token_world/mechanic/seeds/persuade.py (from 04-09 — stub pattern)
    - docs/guides/authoring-mechanics.md §8
  </read_first>
  <behavior>
    contagion:
    - id="contagion"; voluntary=True (Phase 4; Phase 5 tick-end sweep per GAP-ENG07)
    - check: target has `infected=True`; target's room has other agents not yet infected
    - apply: for each uninfected agent in same room, roll probability = target.transmission_rate (e.g., 0.3); if `random.random() < rate` → set infected=True on that agent
    - Determinism: `ctx.seed` / `ctx._seed` is a documented gap (GAP-GRAPH05, see `docs/guides/authoring-mechanics.md` § Known gaps); stub accordingly. For Phase 4, seed a local `random.Random(tick_id if accessible else 42)` instance and document the determinism gap clearly referencing GAP-GRAPH05 in the module docstring.

    weather_reaction:
    - COMMITTED DECISION per D-38: ship as framework-gap STUB with class-level `blocked_by = "GAP-ENG09"`. Do NOT attempt a real WorldPropertyMatcher implementation in this plan. The decision is made at authoring time and recorded in the module source; the stub pattern mirrors persuade.py / cooperate.py from 04-09.
    - tags=["environmental","weather","involuntary"]
    - Deviation policy: if during execution an author discovers GAP-ENG09 is unnecessary (a workaround exists using existing matchers), that is a DEVIATION from this plan — surface it via the execute-phase deviation protocol and revise the plan before implementing; do NOT silently switch from stub to real implementation.
  </behavior>
  <action>
Step A — `contagion.py`: real implementation. Ensure determinism for tests — use `random.Random(seed)` instance. `ctx.seed` / `ctx._seed` is a documented gap (GAP-GRAPH05, see `docs/guides/authoring-mechanics.md` § Known gaps); seed from `tick_id` if accessible or a class-level fallback. Document in docstring: "v1 uses Python random; GAP-GRAPH05 introduces seeded-RNG primitive on ctx in Phase 5 for deterministic replay."

Tests (≥3): one infected + 3 uninfected co-located agents with transmission_rate=1.0 → all infected; rate=0.0 → none infected; target not infected → refuse.

Step B — `weather_reaction.py`: SHIP AS STUB. The module file MUST contain a class `WeatherReaction(Mechanic)` with a class-level attribute `blocked_by: str = "GAP-ENG09"` — this is the committed decision from D-38 / this plan's must_haves. Pattern mirrors persuade.py / cooperate.py from 04-09. Tests (≥2): (1) the stub is discoverable by `MechanicRegistry`; (2) `check()` returns a refusal whose reason string contains "GAP-ENG09". Do NOT write a "real" implementation behind a feature flag — the stub IS the plan output; a real implementation is Phase 5's job.

Step C — Verify harness routing: UC-V02 and UC-V04 must remain blocked. If the harness's `_BLOCKED_BY_MAP` routing (from 04-09) correctly skips them with the weather_reaction verb, confirm:
```bash
uv run pytest tests/test_integration/test_use_cases.py -k "UC-V02 or UC-V04" -q 2>&1 | grep -E "GAP-ENG09|blocked"
```
If the manifest's classified.verb doesn't map to `weather_reaction` (e.g., manifest verb is `change_weather` or `advance_season`), matcher behavior is owned by 04-04 (`tests/test_integration/test_use_cases.py::match_mechanic_for_verb`, contract test-covered in `tests/test_mechanic/test_harness_matcher.py`). Do NOT add a tag-based fallback from this plan. If UC-V02/UC-V04 cannot route to the stub without such a fallback, STOP and open a decimal plan before 04-04 per 04-04-SUMMARY Extension Contract. In-plan pragmatic fix: align the manifest's classified.verb with the mechanic id (`weather_reaction`) so the exact-id match routes correctly.
  </action>
  <acceptance_criteria>
    - `src/token_world/mechanic/seeds/contagion.py` + `weather_reaction.py` exist and validate
    - `grep -n "blocked_by" src/token_world/mechanic/seeds/weather_reaction.py` returns ≥1 match (W6: the stub decision is committed at authoring time, not execution)
    - `grep -n "blocked_by: str = \"GAP-ENG09\"\\|blocked_by = \"GAP-ENG09\"" src/token_world/mechanic/seeds/weather_reaction.py` returns ≥1 match (exact string)
    - `grep -n "GAP-GRAPH05\\|seeded-RNG\\|seeded RNG" src/token_world/mechanic/seeds/contagion.py` returns at least one match (gap citation)
    - Tests: ≥3 for contagion, ≥2 for weather_reaction; all pass
    - `uv run pytest tests/test_integration/test_use_cases.py -k "UC-V02 or UC-V04" -q` reports both SKIPPED with GAP-ENG09 in reason (OR correctly-blocked via manifest's original expected_outcome)
  </acceptance_criteria>
  <verify>
    <automated>for m in contagion weather_reaction; do uv run token-world validate-mechanic src/token_world/mechanic/seeds/$m.py || exit 1; done && uv run pytest tests/test_mechanic/test_seeds/test_contagion.py tests/test_mechanic/test_seeds/test_weather_reaction.py -x -q</automated>
  </verify>
  <done>
    2 mechanics shipped (1 real + 1 stub); harness correctly blocks UC-V02/V04.
  </done>
</task>

<task type="auto">
  <name>Task 3: Flip UC-V01, UC-V03, UC-V06, UC-V07 to pass + VALIDATION.md + full suite</name>
  <read_first>
    - Each UC manifest
    - tests/test_integration/test_use_cases.py
  </read_first>
  <action>
Step A — Flip UC-V01, UC-V03, UC-V06, UC-V07 `expected_outcome: yield` → `pass`. Verify manifest setup + actions can actually be satisfied by the Phase 4 mechanics. If UC-V03's manifest expects passive tick-end decay (no actor verb), leave `blocked` (GAP-ENG07).

Step B — Run:
```bash
uv run pytest tests/test_integration/test_use_cases.py -k "UC-V01 or UC-V02 or UC-V03 or UC-V04 or UC-V06 or UC-V07" -q
uv run pytest -x -q
```

Step C — Append VALIDATION.md rows:

| 04-11-T1 | 04-11 | 4 | MECH-03 | — | MECH20/22/23 fire_spread + decay_tick + illumination | unit | `uv run pytest tests/test_mechanic/test_seeds/test_fire_spread.py tests/test_mechanic/test_seeds/test_decay_tick.py tests/test_mechanic/test_seeds/test_illumination.py -x -q` | ✓ | ⬜ pending |
| 04-11-T2 | 04-11 | 4 | MECH-03 | — | MECH24 contagion (real) + MECH21 weather_reaction (stub, GAP-ENG09) | unit | `uv run pytest tests/test_mechanic/test_seeds/test_contagion.py tests/test_mechanic/test_seeds/test_weather_reaction.py -x -q` | ✓ | ⬜ pending |
| 04-11-T3 | 04-11 | 4 | MECH-03, TEST-02 | — | UC-V01/03/06/07 flip to pass; UC-V02/04 correctly blocked by GAP-ENG09 | integration | `uv run pytest tests/test_integration/test_use_cases.py -k "UC-V01 or UC-V02 or UC-V03 or UC-V04 or UC-V06 or UC-V07" -q` | ✓ | ⬜ pending |
  </action>
  <acceptance_criteria>
    - UC-V01, UC-V06, UC-V07 flipped (grep check)
    - UC-V03 either flipped to `pass` (if manifest's actions include an explicit decay invocation) OR remains `blocked` with GAP-ENG07 rationale documented in summary
    - UC-V02, UC-V04 remain `blocked`
    - `uv run pytest tests/test_integration/test_use_cases.py -k "UC-V01 or UC-V06 or UC-V07" -q` reports 3 passed
    - `uv run pytest -x -q` exits 0
    - `grep -c "04-11-T" .planning/phases/04-llm-mechanic-generation/04-VALIDATION.md` returns ≥3
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_integration/test_use_cases.py -k "UC-V01 or UC-V06 or UC-V07" -q | grep -E "3 passed" && uv run pytest -x -q</automated>
  </verify>
  <done>
    5 mechanics + UC status updates; full suite green.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-04-AST-BYPASS | Tampering | New mechanic modules | accept | Validation pipeline runs on each. |
| T-04-CYCLE | DoS | fire_spread + illumination (involuntary cascade) | mitigate | Both mechanics' check() refuses to re-fire on already-affected nodes (e.g., on_fire=True already). The engine's max_chain_depth=10 is the backstop. Verifiable: test `test_fire_spread_does_not_reignite_already_burning`. |
</threat_model>

<verification>
- 5 new mechanics validate
- UC-V01/06/07 pass; UC-V02/04 blocked; UC-V03 classified
- Full pytest green
</verification>

<success_criteria>
- GAP-MECH20/22/23/24 closed (real)
- GAP-MECH21 addressed as stub blocked on GAP-ENG09
- Reactive-cycle guards in fire_spread + illumination (GAP-MECH26 applied practice)
</success_criteria>

<output>
Create `.planning/phases/04-llm-mechanic-generation/04-11-SUMMARY.md` listing: mechanic ids, stub mappings, UCs flipped, UC-V03 classification rationale.
</output>
