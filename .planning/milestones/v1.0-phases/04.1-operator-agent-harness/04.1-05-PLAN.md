---
phase: 04.1
plan: 05
title: "Interactive entry-point polish — universe CLAUDE.md additions, .claude/agents/mechanic-author.md scaffold, VALIDATION.md finalisation"
type: execute
wave: 4
depends_on:
  - "04.1-03"
files_modified:
  - src/token_world/universe/scaffold.py
  - src/token_world/universe/templates/claude_md.py
  - src/token_world/universe/templates/mechanic_author_agent.py
  - tests/test_universe/test_scaffold.py
  - .planning/phases/04.1-operator-agent-harness/04.1-VALIDATION.md
  - docs/design/architecture.md
autonomous: false
requirements:
  - UNIV-03
  - AUTO-02
decisions:
  - D-05
  - D-12
  - D-19
tags:
  - operator
  - scaffolding
  - claude-md
  - interactive

must_haves:
  truths:
    - "A newly-scaffolded universe contains a `.claude/agents/mechanic-author.md` file with prompt content sourced from the shared mechanic_author_prompt() function"
    - "The universe's CLAUDE.md has an 'Operator Flow: When a Tick Yields' section explaining the canonical flow"
    - "The CLAUDE.md 'Available Tools' section accurately reflects the Phase 4 3-tool surface (resume_tick / rollback / list_mechanics) and drops stale 'Not yet implemented' notes where tools now exist"
    - "The universe's .mcp.json continues to expose exactly 3 MCP tools (UNIV-03 preserved, D-06 honoured)"
    - "VALIDATION.md's per-task map is populated for every 04.1 task with Automated Command + Status; pytest marker canonicalised to `integration`"
    - "docs/design/architecture.md includes an updated Mermaid diagram showing the operator harness, yield-signal contract, diagnostics namespace, and dual entry points"
    - "A human operator can open Claude Code inside a scaffolded universe, run `token-world run-tick --manual`, and follow clear instructions to complete the loop — verified by a documented manual smoke test"
  artifacts:
    - path: "src/token_world/universe/templates/mechanic_author_agent.py"
      provides: "render_mechanic_author_md(universe_path) -> str using mechanic_author_prompt() from operator.subagent"
      contains: "def render_mechanic_author_md"
    - path: "src/token_world/universe/scaffold.py"
      provides: "Writes .claude/agents/mechanic-author.md during universe creation"
      contains: ".claude/agents"
    - path: "src/token_world/universe/templates/claude_md.py"
      provides: "Updated template with Operator Flow section + accurate tool documentation"
      contains: "Operator Flow"
    - path: ".planning/phases/04.1-operator-agent-harness/04.1-VALIDATION.md"
      provides: "Populated per-task map; all 04.1-01..05 tasks have automated verify commands + status; nyquist_compliant: true"
    - path: "docs/design/architecture.md"
      provides: "Mermaid diagram + section covering operator harness, dual entry points, yield signal contract"
  key_links:
    - from: "src/token_world/universe/scaffold.py"
      to: ".claude/agents/mechanic-author.md (per-universe)"
      via: "Writes file during scaffold_universe()"
      pattern: ".claude/agents/mechanic-author.md"
    - from: "src/token_world/universe/templates/mechanic_author_agent.py"
      to: "src/token_world/operator/subagent.py"
      via: "imports mechanic_author_prompt() — single source of truth"
      pattern: "mechanic_author_prompt"
    - from: "src/token_world/universe/templates/claude_md.py"
      to: "Operator Flow section"
      via: "CLAUDE.md template string"
      pattern: "Operator Flow"
---

<objective>
Close Phase 4.1 by making the interactive (Claude-Code-in-universe-folder) entry point Just Work: update the CLAUDE.md template so Claude Code reads canonical operator-flow guidance on session start; scaffold `.claude/agents/mechanic-author.md` per universe with the SAME prompt content the programmatic harness uses, so the human path and programmatic path are truly parity. Finalise VALIDATION.md's per-task map. Refresh architecture docs.

Purpose: D-05 makes dual entry points first-class. Without this plan's scaffolding updates, Claude Code in a universe folder has no idea there's a `mechanic-author` subagent to invoke, and ad-hoc universes miss the Operator Flow narrative. Prompt drift between programmatic and interactive paths is the single biggest failure mode — we avoid it by sourcing both prompts from `mechanic_author_prompt()` (defined in 04.1-03).

Output: Scaffolder emits `.claude/agents/mechanic-author.md`; CLAUDE.md template documents the yield flow; VALIDATION.md is audit-ready with `nyquist_compliant: true`; architecture diagram reflects Phase 4.1 additions; a manual smoke test checklist is recorded for a human to execute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-operator-agent-harness/04.1-CONTEXT.md
@.planning/phases/04.1-operator-agent-harness/04.1-RESEARCH.md
@.planning/phases/04.1-operator-agent-harness/04.1-03-SUMMARY.md
@.planning/phases/04.1-operator-agent-harness/04.1-VALIDATION.md
@src/token_world/operator/subagent.py
@src/token_world/universe/scaffold.py
@src/token_world/universe/templates/claude_md.py
@src/token_world/universe/templates/mcp_config.py
@docs/design/architecture.md

<interfaces>
<!-- New helper API -->

```python
# src/token_world/universe/templates/mechanic_author_agent.py
from pathlib import Path

def render_mechanic_author_md(universe: Path) -> str:
    """Produce the `.claude/agents/mechanic-author.md` body.

    Structure (Claude Code filesystem-agent format):
        ---
        description: Authors a new mechanic in response to a simulation yield.
        tools: Read, Write, Edit, Glob, Grep, Bash, mcp__validation__validate_mechanic, mcp__token-world__list_mechanics
        model: opus
        ---

        {body from mechanic_author_prompt()}

    The prompt body is ALWAYS sourced from token_world.operator.subagent.mechanic_author_prompt
    so interactive and programmatic paths never drift.
    """
```

Scaffolding hook: `scaffold_universe` writes `.claude/agents/mechanic-author.md` in addition to the existing CLAUDE.md / .mcp.json / git init steps.

CLAUDE.md additions (appended after existing "Available Tools" section; verbatim from RESEARCH §Pattern 8):

```markdown
## Operator Flow: When a Tick Yields

If `token-world run-tick` (or `resume_tick` returns a yield result), the
simulation has halted because no mechanic matches the resident agent's action.
Your job is to author the needed mechanic and resume the tick.

Canonical flow:

1. Run `token-world inspect-yield` to see the yield signal (classified action,
   actor state, candidate existing mechanics).
2. Use the `mechanic-author` subagent (defined in `.claude/agents/mechanic-author.md`)
   to write the mechanic. The subagent reads the authoring guide, writes
   `mechanics/<id>.py` + a test, and validates with `validate-mechanic` until
   passing.
3. Run `token-world resume-tick --tick <tick_id>` to continue. The registry
   re-scans, picks up the new mechanic, and the tick completes.

When to author vs extend: if the yield's `candidate_mechanic_ids` list is
populated, Read those files first — prefer extending over rewriting when the
verb semantics match.

Debugging past ticks: `token-world replay-tick <tick_id>` renders the full
per-tick diagnostics.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mechanic-author scaffold template + scaffold-time write + CLAUDE.md Operator Flow section</name>
  <files>
    src/token_world/universe/templates/mechanic_author_agent.py,
    src/token_world/universe/templates/claude_md.py,
    src/token_world/universe/scaffold.py,
    tests/test_universe/test_scaffold.py
  </files>
  <read_first>
    src/token_world/universe/scaffold.py (full file — understand existing scaffold_universe sequence),
    src/token_world/universe/templates/claude_md.py (existing template string; where to insert Operator Flow section),
    src/token_world/operator/subagent.py (Plan 03 output — mechanic_author_prompt function),
    .planning/phases/04.1-operator-agent-harness/04.1-RESEARCH.md §"Pattern 5" (interactive parity) + §"Pattern 8" (CLAUDE.md additions — use the markdown verbatim),
    .planning/phases/04.1-operator-agent-harness/04.1-RESEARCH.md §"Assumptions Log A4" (filesystem agent location)
  </read_first>
  <behavior>
    - Test `test_render_mechanic_author_md_has_yaml_frontmatter`: output starts with `---\n` and contains `description:`, `tools:`, `model: opus` frontmatter keys.
    - Test `test_render_mechanic_author_md_body_contains_shared_prompt`: the body section (after frontmatter) contains a substantial substring of `mechanic_author_prompt(universe=..., yield_json="<YIELD_SIGNAL_JSON>")` — confirming the shared source. Use a stable sentinel phrase from the prompt (e.g., "Token World Mechanic Author") to assert against.
    - Test `test_render_mechanic_author_md_tools_exclude_Agent`: YAML `tools:` line does NOT include `Agent` (filesystem agent Pitfall 5).
    - Test `test_render_mechanic_author_md_tools_include_validate_mechanic`: `tools:` mentions `mcp__validation__validate_mechanic`.
    - Test `test_scaffold_writes_mechanic_author_md`: after `scaffold_universe(path, name="X", slug="x")`, the file `<universe>/.claude/agents/mechanic-author.md` exists and is non-empty.
    - Test `test_scaffold_mechanic_author_md_roundtrips_frontmatter`: parse the YAML frontmatter (using `yaml.safe_load` on the content between the first two `---`) and assert `frontmatter["model"] == "opus"`.
    - Test `test_scaffold_dot_claude_dir_created`: `<universe>/.claude/agents/` directory exists after scaffold.
    - Test `test_claude_md_contains_operator_flow_section`: `render_claude_md(name="X")` output contains `## Operator Flow: When a Tick Yields`.
    - Test `test_claude_md_mentions_three_mcp_tools_accurately`: Available Tools section mentions all 3 (resume_tick, rollback, list_mechanics).
    - Test `test_claude_md_does_not_mention_register_mechanic`: the string "register_mechanic" is absent (Phase 4 D-19 dropped it).
    - Test `test_claude_md_mentions_mechanic_author_subagent`: body references `.claude/agents/mechanic-author.md`.
    - Test `test_claude_md_operator_flow_references_cli_commands`: contains `inspect-yield`, `resume-tick`, `replay-tick`.
  </behavior>
  <action>
    Step A — Create `src/token_world/universe/templates/mechanic_author_agent.py`:

    ```python
    """Renderer for .claude/agents/mechanic-author.md scaffold file.

    Per D-05 / RESEARCH Pattern 5: the filesystem agent is sourced from the
    same `mechanic_author_prompt` function the programmatic AgentDefinition
    uses. Prompt drift between interactive and programmatic operator paths is
    eliminated by this shared source.
    """
    from __future__ import annotations

    from pathlib import Path

    from token_world.operator.subagent import mechanic_author_prompt


    _FRONTMATTER = """\
    ---
    description: Authors a new mechanic in response to a simulation yield. Invoke when the simulation has halted waiting for a mechanic matching the resident agent's classified action.
    tools: Read, Write, Edit, Glob, Grep, Bash, mcp__validation__validate_mechanic, mcp__token-world__list_mechanics
    model: opus
    ---

    """

    _TEMPLATE_NOTE = """\
    > **Note:** In this filesystem-agent form, `<YIELD_SIGNAL_JSON>` below is a
    > template placeholder. At invocation time, paste the live yield signal
    > (run `token-world inspect-yield --format json`) into the subagent prompt.

    """


    def render_mechanic_author_md(universe: Path) -> str:
        body = mechanic_author_prompt(
            universe=universe,
            yield_json="<YIELD_SIGNAL_JSON>",
        )
        return _FRONTMATTER + _TEMPLATE_NOTE + body
    ```

    Step B — Update `src/token_world/universe/scaffold.py`. Add a helper and call it from `scaffold_universe`:

    ```python
    from token_world.universe.templates.mechanic_author_agent import render_mechanic_author_md

    def _write_mechanic_author_agent(universe_dir: Path) -> None:
        agents_dir = universe_dir / ".claude" / "agents"
        agents_dir.mkdir(parents=True, exist_ok=True)
        (agents_dir / "mechanic-author.md").write_text(
            render_mechanic_author_md(universe_dir), encoding="utf-8",
        )
    ```

    Invoke `_write_mechanic_author_agent(universe_dir)` from `scaffold_universe` AFTER the CLAUDE.md / .mcp.json writes but BEFORE the git init commit (so the initial commit includes the agent file).

    Step C — Update `src/token_world/universe/templates/claude_md.py`:
    1. Append the Operator Flow section text (from interfaces block, verbatim RESEARCH Pattern 8) at the end of the existing template.
    2. Audit the Available Tools section:
       - DELETE the `### register_mechanic` subsection (Phase 4 D-19 dropped it).
       - UPDATE `### resume_tick`, `### rollback`, `### list_mechanics` subsections: if Phase 4 implemented these, remove the "Not yet implemented" stamps and replace with a one-line description. For `resume_tick`, note "may yield a YieldSignal; see Operator Flow section below".
    3. Add a brief preamble line near the top linking to the Operator Flow section.

    Step D — Extend `tests/test_universe/test_scaffold.py` with the behaviours listed. For YAML parsing:
    ```python
    import yaml
    text = (universe / ".claude/agents/mechanic-author.md").read_text()
    parts = text.split("---", 2)
    frontmatter = yaml.safe_load(parts[1])
    assert frontmatter["model"] == "opus"
    ```

    Step E — Quality gates:
    - `uv run pytest tests/test_universe/ -x -q`
    - `uv run mypy src/token_world/universe/`
    - `uv run ruff check src/token_world/ tests/test_universe/`

    Step F — Dogfood: `uv run token-world create test-scaffold-v2 && cat $(python -c "from token_world.universe.manager import UniverseManager; print(UniverseManager().load('test-scaffold-v2'))")/.claude/agents/mechanic-author.md`. Verify the agent markdown reads naturally.
  </action>
  <verify>
    <automated>uv run pytest tests/test_universe/ -x -q &amp;&amp; uv run mypy src/token_world/universe/</automated>
  </verify>
  <done>
    Newly scaffolded universes include `.claude/agents/mechanic-author.md` with YAML frontmatter and body sourced from `mechanic_author_prompt()`. CLAUDE.md includes the Operator Flow section, drops stale `register_mechanic` mentions, and points to the mechanic-author subagent. All universe scaffold tests green; mypy + ruff clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Finalise VALIDATION.md per-task map + canonicalise integration marker + update architecture diagram + human smoke test</name>
  <files>
    .planning/phases/04.1-operator-agent-harness/04.1-VALIDATION.md,
    docs/design/architecture.md
  </files>
  <read_first>
    .planning/phases/04.1-operator-agent-harness/04.1-VALIDATION.md (current draft — has skeleton),
    .planning/phases/04.1-operator-agent-harness/04.1-01-SUMMARY.md through 04.1-04-SUMMARY.md,
    .planning/phases/04.1-operator-agent-harness/04.1-01-PLAN.md through 04.1-04-PLAN.md (task IDs + verify commands),
    docs/design/architecture.md (current diagram — understand the existing Mermaid style to match)
  </read_first>
  <acceptance_criteria>
    - VALIDATION.md has a populated per-task table with one row per task across plans 01..05 (approximately 11-13 rows total).
    - Each row has: Task ID (e.g. 04.1-01-01), Plan, Wave, Requirement, Threat Ref, Test Type, Automated Command, File Exists, Status.
    - Frontmatter has `nyquist_compliant: true` and `status: finalized`.
    - Every row's Automated Command actually executes and returns 0 (integration tests skip cleanly without ANTHROPIC_API_KEY).
    - Pytest marker canonicalised to `integration` throughout (draft mixed `slow`/`integration`; the registered marker per pyproject.toml is `integration`).
    - Manual-Only Verifications section expanded with step-by-step human smoke test.
    - `docs/design/architecture.md` has a new "Phase 4.1: Operator Harness" section with a Mermaid diagram showing YieldSignal, EngineStub (test-only, dashed), OperatorHarness, mechanic-author subagent, validate-mechanic @tool, DiagnosticsSink/operator namespace, and the two entry points (CLI run-tick + Claude Code interactive).
    - Mermaid diagram rendered via `mcp-mermaid` MCP; reviewer confirms no spaghetti edges / overlapping labels / unreadable text (per global operating principle "close the feedback loop").
    - Human smoke test executed: operator runs the documented steps in a fresh universe, confirms yield→author→resume completes end-to-end without hand-writing mechanic code. Status column in VALIDATION.md updated to reflect the result.
  </acceptance_criteria>
  <what-built>
    Plans 01–04 have shipped the operator harness end-to-end. Task 1 of this plan shipped the universe scaffolding polish.
    This task: populate VALIDATION.md so the phase is audit-ready, update architecture docs with a rendered Mermaid diagram, and drive one human smoke test of the interactive entry point.
  </what-built>
  <how-to-verify>
    **Step A — VALIDATION.md per-task map.** For each task in `04.1-01-PLAN.md` through `04.1-04-PLAN.md`, add one row to the per-task table. Task IDs follow pattern `04.1-{plan}-{task_n}`.

    **IMPORTANT — Requirement column fix:** VALIDATION.md's existing draft rows use `AGENT-03 / AGENT-04` as the Requirement cell. Those are Phase 6 requirements, NOT Phase 4.1 per ROADMAP.md (corrected in this revision pass). Rewrite every row's Requirement cell to reference ONLY the requirements this phase covers:

    - `AUTO-02` — for rows covering diagnostics writes (Plan 02 tasks, Plan 03 Task 2 harness diagnostics-wiring test)
    - `UNIV-03` — for rows covering the 3-tool surface (Plan 01 YieldSignal contract, Plan 03 Task 2 three-MCP-tools-whitelist test, Plan 04 CLI commands that don't add new MCP tools, Plan 05 scaffolding tests asserting 3 tools in `.mcp.json`)
    - Purely Wave-0 scaffolding rows (e.g. `04.1-01-01` dep install + pytest marker) may carry `UNIV-03` (the YieldSignal locks the 3-tool contract's sufficiency) OR be left blank with a note — pick the consistent option phase-wide.

    Template row:

    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|

    Starting rows (adjust to actual task counts after reading the plans):
    - 04.1-01-01 through -03 (Plan 01: dep install, YieldSignal, EngineStub)
    - 04.1-02-01 through -02 (Plan 02: OperatorDiagnosticsContext, Reader + Sink integration)
    - 04.1-03-01 through -03 (Plan 03: validation @tool + subagent, harness, integration test)
    - 04.1-04-01 through -02 (Plan 04: cli_support, CLI commands)
    - 04.1-05-01 (Plan 05 Task 1: scaffold updates)
    - 04.1-05-02 (this task: manual smoke — status pending until operator runs it)

    **Step B — Frontmatter update** in VALIDATION.md:
    ```yaml
    ---
    phase: 04.1
    slug: operator-agent-harness
    status: finalized
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-12
    finalized: <today's date>
    ---
    ```

    **Step C — Marker canonicalisation.** Replace all `slow` marker references with `integration` across VALIDATION.md (the registered pytest marker in `pyproject.toml` is `integration` per Plan 01). Update commands like `pytest -m slow` → `pytest -m integration`. Verify consistency across the whole file.

    **Step D — Expand Manual-Only Verifications** with these exact steps:
    1. `uv run token-world create test-operator-interactive`
    2. `cd` into the created universe folder.
    3. Seed a halted tick: `token-world run-tick --stub verb=dig --stub actor=alice --manual` (writes yield_signal.json but skips the subagent).
    4. Launch Claude Code: `claude`.
    5. Prompt Claude Code: "I just ran run-tick and got a yield. Please handle it per the Operator Flow in CLAUDE.md."
    6. Observe: Claude Code reads CLAUDE.md's Operator Flow section → runs `token-world inspect-yield` → invokes the `mechanic-author` subagent → subagent writes mechanic + validates → Claude Code calls `token-world resume-tick --tick tick_1`.
    7. Pass criteria: (a) `mechanics/<id>.py` exists, (b) `token-world replay-tick tick_1` shows `success=True`, (c) no human code-writing required, (d) **observed cost recorded in VALIDATION.md smoke-test row — flag if > $5**. Cost-cap enforcement authority is the Plan 03 BLOCKER-4 resolution: if `max_budget_usd` is a real ClaudeAgentOptions field, the SDK hard-caps at $5; otherwise, `max_turns=20` is the hard cap and loguru warns at $3 and $5. The smoke test's role is to *observe* and *record*, not to re-enforce.

    Record pass/fail in the Status column. On failure, file a gap (using `/gsd-plan-phase --gaps`) and iterate.

    **Step E — Architecture diagram.** Append to `docs/design/architecture.md`:

    ````markdown
    ## Phase 4.1: Operator Harness

    The operator harness catches simulation yields and drives mechanic authoring via the Claude Agent SDK. Two entry points share the same universe MCP surface and the same mechanic-author subagent definition.

    ```mermaid
    flowchart TB
        subgraph Engines["Yield sources"]
            Phase5["Phase 5 engine<br/>(not built)"]
            Stub["EngineStub<br/>(test-only)"]
        end

        subgraph Entry["Entry points"]
            CLI["token-world run-tick<br/>(Click CLI)"]
            CC["Claude Code<br/>(CLAUDE.md-driven)"]
        end

        Harness["OperatorHarness<br/>(Agent SDK, Opus)"]

        subgraph SDKSession["Outer SDK session"]
            direction LR
            Subagent["mechanic-author<br/>subagent (Opus)"]
            ValTool["validate_mechanic<br/>@tool wrapper"]
            MCP3["3-tool MCP surface<br/>resume_tick / rollback / list_mechanics"]
        end

        Diag["DiagnosticsSink<br/>+ operator/ namespace<br/>(Phase 4.1 extension)"]
        Mechanics["mechanics/*.py<br/>+ tests/"]

        Phase5 -. "YieldSignal" .-> Harness
        Stub -. "YieldSignal (test)" .-> Harness
        CLI --> Harness
        CC --> Harness
        Harness --> Subagent
        Subagent --> ValTool
        Subagent --> Mechanics
        Harness --> MCP3
        Harness --> Diag
    ```

    Key contracts:
    - **YieldSignal** (`src/token_world/operator/yield_signal.py`) is the locked interface between engine and operator; frozen dataclass, JSON-round-trippable, schema-versioned.
    - **mechanic-author subagent prompt** (`src/token_world/operator/subagent.py::mechanic_author_prompt`) is the single source of truth; both the programmatic `AgentDefinition` and the per-universe `.claude/agents/mechanic-author.md` use it.
    - **Diagnostics** (`src/token_world/operator/diagnostics.py`) extends Phase 4's `DiagnosticsSink` with an `operator/` subfolder per tick (yield signal, authoring attempts JSONL, per-attempt validation reports, mechanic diff, resume outcome).
    ````

    Render via `mcp-mermaid` MCP to validate layout. Iterate until no spaghetti edges / overlapping labels.

    **Step F — Cross-document sanity check.** Read `CLAUDE.md` (project root) — it should already describe Agent SDK at operator layer correctly. No changes needed unless a factual inaccuracy is found. Do NOT modify `.planning/STATE.md`; that's the orchestrator's job post-phase.

    **Step G — Verify the verify commands.** For each row in the per-task map, run the `Automated Command` and confirm exit 0 (or graceful skip for integration). If any command doesn't pass, the plan is incomplete — investigate before marking the row green.
  </how-to-verify>
  <resume-signal>
    Type `approved` after VALIDATION.md is finalised (nyquist_compliant: true), architecture.md has the rendered Mermaid diagram reviewed, and the manual smoke-test row has a concrete pass/fail status. If smoke test failed, describe what went wrong so we can plan gap closure.
  </resume-signal>
  <action>
    See `<how-to-verify>` above — this is a checkpoint:human-verify task. The executor performs Steps A–G (populate VALIDATION.md per-task map, canonicalise integration marker, expand manual-only verifications, append Phase 4.1 section with Mermaid diagram to docs/design/architecture.md, render diagram via mcp-mermaid, then drive the human smoke test), then pauses for user sign-off.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/04.1-operator-agent-harness/04.1-VALIDATION.md &amp;&amp; grep -q "Phase 4.1: Operator Harness" docs/design/architecture.md &amp;&amp; grep -q "mermaid" docs/design/architecture.md</automated>
  </verify>
  <done>
    VALIDATION.md per-task map populated for all 04.1 tasks; frontmatter `nyquist_compliant: true` and `status: finalized` set; `slow` marker references replaced with `integration`; Manual-Only Verifications section has step-by-step human smoke test; docs/design/architecture.md has a new "Phase 4.1: Operator Harness" section with a rendered-and-reviewed Mermaid diagram; human smoke test run with pass/fail recorded in VALIDATION.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scaffolding → `.claude/agents/mechanic-author.md` | The filesystem-agent file controls subagent behaviour when Claude Code is opened in a universe. Incorrect prompt content directly affects authoring quality. |
| CLAUDE.md template → Claude Code instructions | CLAUDE.md is read on every session start; errors here silently mis-guide the interactive operator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-22 | Tampering | Prompt drift between programmatic AgentDefinition and filesystem `.claude/agents/mechanic-author.md` | mitigate | Single source via `mechanic_author_prompt()` — scaffold imports this function rather than duplicating text. Test `test_render_mechanic_author_md_body_contains_shared_prompt` asserts the shared-prompt invariant. |
| T-04.1-23 | Tampering | `.claude/agents/mechanic-author.md` accidentally includes `Agent` in `tools` (allowing sub-subagents) | mitigate | Frontmatter template hardcodes the tools line; test asserts Agent absent. |
| T-04.1-24 | Spoofing | Adversarial `.claude/agents/mechanic-author.md` overriding the scaffolded one | accept | Universe folders are developer-local and trusted; tampering requires local filesystem access beyond this threat model. |
| T-04.1-25 | Information Disclosure | CLAUDE.md revealing internal implementation details that help an adversarial prompt-injection payload pivot | accept | CLAUDE.md content is already public via the project repo; no secrets. The universe's mechanics/* files may include user-authored code but that's intentional per the "universe as codebase" design. |
</threat_model>

<verification>
- Scaffolding: `uv run token-world create scaffold-smoke && ls universes/scaffold-smoke/.claude/agents/mechanic-author.md`
- Frontmatter: `head -5 universes/scaffold-smoke/.claude/agents/mechanic-author.md` shows YAML frontmatter with `model: opus`
- CLAUDE.md: `grep "Operator Flow" universes/scaffold-smoke/CLAUDE.md` returns a match
- Tests: `uv run pytest tests/test_universe/ -x -q` green
- VALIDATION.md: `grep "nyquist_compliant: true" .planning/phases/04.1-operator-agent-harness/04.1-VALIDATION.md` returns a match
- Architecture: `grep -A 2 "Phase 4.1: Operator Harness" docs/design/architecture.md` shows the new section
- Mermaid diagram rendered via mcp-mermaid and visually reviewed
- Manual smoke test: operator confirms end-to-end flow works interactively in Claude Code
</verification>

<success_criteria>
- Phase 4.1 CONTEXT Success Criterion #2 achieved: "the same yield→author→resume loop completes from an interactive Claude Code session inside a universe folder"
- Both entry points source their subagent prompt from a single Python function (no drift possible)
- VALIDATION.md is audit-ready: per-task map populated, nyquist_compliant: true, marker canonicalised
- Architecture documentation reflects the new operator harness shape
- Human smoke test executed with pass/fail recorded
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-operator-agent-harness/04.1-05-SUMMARY.md`:
- Final template excerpts (CLAUDE.md Operator Flow section + `.claude/agents/mechanic-author.md` frontmatter)
- Mermaid diagram source (for easy reference)
- Manual smoke test result (pass / fail + any gaps filed)
- VALIDATION.md finalisation checklist completion
- Any deviations from RESEARCH §Pattern 5 / §Pattern 8 discovered during implementation
</output>
